---
phase: 02-read-tools-schema-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - src/keeping/types.ts
  - src/keeping/errors.ts
  - test/keeping/errors.test.ts
autonomous: false
requirements:
  - SAFE-04
user_setup: []

must_haves:
  truths:
    - "Decisions implemented in this plan: D-22, D-23, D-24, D-25, D-27, D-37"
    - "All three Phase 2 runtime deps and the tsx devDep are installed at their RESEARCH-pinned versions"
    - "User has visually verified each new dep's npm package name + GitHub repo + maintainer before install (slopcheck unavailable per RESEARCH §Package Legitimacy Audit)"
    - "src/keeping/errors.ts exports KeepingAuthError, KeepingRateLimitError, KeepingApiError, MultiOrgError, sanitiseBody, toIsErrorContent"
    - "KeepingAuthError.message is byte-identical to D-25 wording: 'Keeping rejected the token. Verify KEEPING_TOKEN and restart the MCP server.'"
    - "MultiOrgError.message follows D-27 template: 'Multiple organisations available. Pass organisation_id, or set KEEPING_ORG_ID. Options: <id> (<name>), <id> (<name>).'"
    - "sanitiseBody(body, token) replaces every token occurrence with '***' (defence-in-depth for Pitfall G token leak in API error bodies)"
    - "src/keeping/types.ts exports loose KeepingUser, KeepingOrg interfaces (D-34 — kept loose by design)"
    - ".gitignore contains '.planning/research/.live-capture-raw.json' and '.planning/research/.probe-raw-*.json' (D-37)"
    - "No production code imports from src/keeping/client.ts or src/server.ts yet — those files do not exist in this plan"
  artifacts:
    - path: "src/keeping/types.ts"
      provides: "KeepingUser, KeepingOrg loose TS interfaces (no runtime validation)"
      min_lines: 10
    - path: "src/keeping/errors.ts"
      provides: "Error classes + sanitiseBody + toIsErrorContent helper"
      min_lines: 30
    - path: "test/keeping/errors.test.ts"
      provides: "Vitest unit tests asserting exact D-25 + D-27 wording and token scrubbing"
      min_lines: 30
    - path: ".gitignore"
      provides: "Augmented ignore rules per D-37"
      contains: ".planning/research/.live-capture-raw.json"
    - path: "package.json"
      provides: "dependencies + devDependencies entries for @modelcontextprotocol/sdk, p-throttle, p-retry, tsx"
      contains: "@modelcontextprotocol/sdk"
  key_links:
    - from: "src/keeping/errors.ts"
      to: "logger redaction primitive in src/logger.ts:20"
      via: "String.prototype.replaceAll(token, '***') — same primitive applied at error-construction-time"
      pattern: "replaceAll.*\\*\\*\\*"
    - from: "test/keeping/errors.test.ts"
      to: "exact-string assertion of D-25 and D-27 messages"
      via: "toBe / toContain with the verbatim CONTEXT-quoted strings"
      pattern: "Keeping rejected the token|Multiple organisations available"
    - from: "package.json devDependencies"
      to: "scripts/probe-live.ts (Plan 02-05)"
      via: "tsx is the runner for the probe-live npm script"
      pattern: "tsx"
---

<objective>
Wave 1 of Phase 2: install the new dependencies (with a slopcheck-fallback human-verify gate),
augment `.gitignore` per D-37, and ship the leaf contracts (`src/keeping/types.ts` +
`src/keeping/errors.ts`) that every downstream plan in this phase imports from. No production
HTTP code, no MCP code, no tools yet — this plan delivers the foundation other plans build on.

Purpose: front-load every decision that has byte-identical-string requirements (D-25, D-27) so
later plans can simply re-use the error classes; front-load every install (single human-verify
checkpoint instead of one per plan); front-load `.gitignore` so the probe-live raw capture in
Plan 02-05 can never be accidentally committed.
Output: new dep entries in `package.json` + lockfile, updated `.gitignore`, two new TS modules
under `src/keeping/`, one vitest file at `test/keeping/errors.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-read-tools-schema-discovery/02-CONTEXT.md
@.planning/phases/02-read-tools-schema-discovery/02-RESEARCH.md
@.planning/phases/02-read-tools-schema-discovery/02-PATTERNS.md
@.planning/phases/01-foundation-scaffolding/01-CONTEXT.md
@src/logger.ts
@src/config.ts
@.gitignore
@package.json

<interfaces>
<!-- This plan CREATES the error/types contracts. Downstream plans (02-02..02-05) consume these. -->
<!-- Source of truth for shapes: RESEARCH.md §Code Examples Operation 3 (errors.ts skeleton, lines 854-893) -->
<!-- and PATTERNS.md §src/keeping/errors.ts (lines 295-345). -->

Target export surface of src/keeping/errors.ts:

```typescript
export class KeepingAuthError extends Error { /* D-25 exact message */ }
export class KeepingRateLimitError extends Error { readonly retryAfter: number; /* ... */ }
export class KeepingApiError extends Error { readonly status: number; /* ... */ }
export class MultiOrgError extends Error { /* D-27 exact template */ }
export function sanitiseBody(text: string, token: string): string;
export function toIsErrorContent(err: unknown): { isError: true; content: Array<{ type: "text"; text: string }> };
```

Target export surface of src/keeping/types.ts (intentionally loose per D-34):

```typescript
export interface KeepingUser { id: string; name?: string; email?: string; /* extra fields tolerated */ }
export interface KeepingOrg { id: string; name: string; projects?: boolean; tasks?: boolean; timesheet_mode?: string; }
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify npm package legitimacy before install (slopcheck fallback)</name>
  <read_first>
    - .planning/phases/02-read-tools-schema-discovery/02-RESEARCH.md §Package Legitimacy Audit (lines 185-204)
    - CLAUDE.md §"Recommended Stack" (versions section)
  </read_first>
  <what-built>
    Pre-install gate. No automation yet — this checkpoint exists because slopcheck was unavailable
    in the research environment and every entry in RESEARCH.md §Package Legitimacy Audit is tagged
    [ASSUMED]. Per the audit's planner-mitigation directive, the human must visually confirm each
    package name + source repo + maintainer before installation proceeds.
  </what-built>
  <how-to-verify>
    Open each URL below in a browser and confirm (1) the npm package name shown matches the value
    in this checkpoint, (2) the GitHub repository link on the npm page matches the URL in this
    checkpoint, (3) the publisher / maintainer matches.

    1. `@modelcontextprotocol/sdk` ^1.29.0
       - https://www.npmjs.com/package/@modelcontextprotocol/sdk
       - Repo: https://github.com/modelcontextprotocol/typescript-sdk
       - Publisher: modelcontextprotocol org

    2. `p-throttle` ^8.1.0
       - https://www.npmjs.com/package/p-throttle
       - Repo: https://github.com/sindresorhus/p-throttle
       - Publisher: sindresorhus

    3. `p-retry` ^8.0.0
       - https://www.npmjs.com/package/p-retry
       - Repo: https://github.com/sindresorhus/p-retry
       - Publisher: sindresorhus

    4. `tsx` ^4.22.4 (devDep)
       - https://www.npmjs.com/package/tsx
       - Repo: https://github.com/privatenumber/tsx
       - Publisher: privatenumber

    All four packages MUST have zero postinstall scripts (visible on the npm "Versions" tab under
    "Total Files"). If any package looks suspicious (typo-squatted name, unfamiliar publisher,
    fresh registration, postinstall script) — STOP and report back.
  </how-to-verify>
  <resume-signal>Type "approved" once all four packages visually verified, or report the suspicious one.</resume-signal>
  <action>
    Pre-install gate. No file changes in this task. The executor presents the four package URLs above to the human, waits for the human to type "approved" before Task 2 (the actual install) proceeds. If the human reports a suspicious package, halt the plan and surface the report. Do NOT auto-approve — the legitimacy table in RESEARCH §Package Legitimacy Audit is tagged [ASSUMED] and slopcheck was unavailable.
  </action>
  <verify>
    <human-check>User typed "approved" or equivalent explicit consent.</human-check>
  </verify>
  <done>
    User explicitly approved package legitimacy; no package flagged as suspicious; Task 2 may now run.
  </done>
  <acceptance_criteria>
    - Human has typed "approved" (or equivalent explicit consent)
    - No package was reported as suspicious
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Install Phase 2 dependencies + augment .gitignore</name>
  <read_first>
    - package.json (current — never overwrite the existing `dependencies.zod` entry or the `mcpName` field)
    - .gitignore (current — preserve trailing-newline convention, append new lines at the end)
    - .planning/phases/02-read-tools-schema-discovery/02-RESEARCH.md §"Standard Stack" (lines 137-183) for exact version pins
    - .planning/phases/02-read-tools-schema-discovery/02-PATTERNS.md §".gitignore (MODIFY — D-37)" (lines 624-646)
  </read_first>
  <action>
    Run `npm install @modelcontextprotocol/sdk@^1.29.0 p-throttle@^8.1.0 p-retry@^8.0.0` for
    runtime deps, then `npm install -D tsx@^4.22.4` for the devDep. After install, confirm the
    new entries appear in `package.json` (alphabetised under `dependencies` and `devDependencies`
    respectively). `package-lock.json` will be regenerated automatically.

    Append exactly two lines to `.gitignore` per D-37 (PATTERNS §".gitignore (MODIFY — D-37)"):

        .planning/research/.live-capture-raw.json
        .planning/research/.probe-raw-*.json

    Do NOT add anything else. Do NOT remove or reorder existing entries.

    Do NOT yet add the `probe-live` npm script — that lands in Plan 02-05 with the probe script
    itself. Only the four package entries change in `package.json` here.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); ['@modelcontextprotocol/sdk','p-throttle','p-retry'].forEach(k=>{ if(!p.dependencies[k]) { console.error('MISSING dep',k); process.exit(1); } }); if(!p.devDependencies.tsx) { console.error('MISSING devDep tsx'); process.exit(1); } console.log('deps OK')"</automated>
    Additionally: `grep -F '.planning/research/.live-capture-raw.json' .gitignore && grep -F '.planning/research/.probe-raw-*.json' .gitignore` MUST both find the lines.
  </verify>
  <acceptance_criteria>
    - `package.json` declares `@modelcontextprotocol/sdk`, `p-throttle`, `p-retry` under `dependencies` with versions that satisfy the semver ranges above
    - `package.json` declares `tsx` under `devDependencies`
    - `package-lock.json` exists and contains lockfile entries for all four
    - `npm ci` would succeed (no lockfile / package.json drift)
    - `.gitignore` contains both new lines verbatim, appended after the existing entries
    - Pre-existing `.gitignore` entries are untouched and in their original order
    - `npm run typecheck` still passes (no regression)
  </acceptance_criteria>
  <done>
    Deps installed at pinned versions; `.gitignore` augmented; no other files touched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define KeepingClient leaf contracts (types.ts + errors.ts) with RED-then-GREEN vitest discipline</name>
  <read_first>
    - .planning/phases/02-read-tools-schema-discovery/02-RESEARCH.md §Code Examples Operation 3 (lines 854-893) — errors.ts skeleton
    - .planning/phases/02-read-tools-schema-discovery/02-PATTERNS.md §"src/keeping/types.ts" (lines 246-281) — loose-by-design rationale
    - .planning/phases/02-read-tools-schema-discovery/02-PATTERNS.md §"src/keeping/errors.ts" (lines 283-350) — full target shape with D-25/D-27 exact wording
    - .planning/phases/02-read-tools-schema-discovery/02-CONTEXT.md §"Specific Ideas" (lines 144-152) — exact byte-identical error message strings
    - src/logger.ts (entire file — for the `replaceAll(token, "***")` redaction primitive that sanitiseBody mirrors)
    - test/logger.test.ts (entire file — for the vitest + `vi.spyOn` + exact-string assertion style)
  </read_first>
  <behavior>
    - Test 1: `new KeepingAuthError().message` is byte-identical to `"Keeping rejected the token. Verify KEEPING_TOKEN and restart the MCP server."`
    - Test 2: `new MultiOrgError([{id:'org_abc',name:'Acme Studio'},{id:'org_xyz',name:'Beta BV'}]).message` is byte-identical to `"Multiple organisations available. Pass organisation_id, or set KEEPING_ORG_ID. Options: org_abc (Acme Studio), org_xyz (Beta BV)."`
    - Test 3: `sanitiseBody("response with kp_test_FAKE inline", "kp_test_FAKE")` returns `"response with *** inline"` (no occurrence of the token remains)
    - Test 4: `new KeepingApiError(500, "boom — kp_test_FAKE here").message` does NOT contain `kp_test_FAKE` when constructed via `KeepingApiError(500, sanitiseBody(body, token))` (Pitfall G assertion — the error class is the LAST line of defence)
    - Test 5: `toIsErrorContent(new Error("anything"))` returns `{ isError: true, content: [{ type: "text", text: "anything" }] }`
    - Test 6: `new KeepingRateLimitError(42).retryAfter === 42` and message references "42"
  </behavior>
  <action>
    Follow Phase 1 TDD discipline (D-16 carry-forward): RED commit first, then GREEN commit.

    RED step: create `test/keeping/errors.test.ts` with the six tests above (verbatim assertion
    strings — they are test fixtures of D-25 / D-27 wording). Run `npx vitest run` and confirm all
    six tests FAIL (the modules they import don't exist yet). Commit message: `test(02-01): add failing tests for KeepingClient errors + sanitiser`.

    GREEN step: create `src/keeping/types.ts` per PATTERNS lines 261-277 (KeepingUser, KeepingOrg
    interfaces — loose by D-34). Create `src/keeping/errors.ts` per PATTERNS lines 295-345 with:
    - `KeepingAuthError` — message verbatim per D-25 (CONTEXT line 146).
    - `KeepingRateLimitError(retryAfter)` — readonly `retryAfter`, message `Rate limited; retry after ${retryAfter}s`.
    - `KeepingApiError(status, sanitisedBody)` — readonly `status`, message `Keeping API error ${status}: ${sanitisedBody.slice(0, 500)}`.
    - `MultiOrgError(orgs)` — message follows D-27 template (CONTEXT line 145) with org list joined by `", "` and a trailing period.
    - `sanitiseBody(text, token)` — `text.replaceAll(token, "***")` (mirrors `src/logger.ts:20`).
    - `toIsErrorContent(err)` — accepts `unknown`; returns `{ isError: true, content: [{ type: "text", text: <message> }] }`. If `err instanceof Error` use `err.message`; else `String(err)`.

    All imports use `.js` suffix per project ESM convention (PATTERNS §"ESM Import Style").
    Set `this.name = "..."` on each error class (PATTERNS line 303). No `console.*` calls anywhere.

    Run `npx vitest run` again — all six tests must PASS. Commit message: `feat(02-01): add KeepingClient types + error classes`.

    REFACTOR step (only if needed): tidy. Run `npx biome check .` and `npx tsc --noEmit` — both
    must pass. If any biome violations, fix them; if `noConsole` reports a hit, the code is wrong
    and must be rewritten.
  </action>
  <verify>
    <automated>npx vitest run test/keeping/errors.test.ts && npx tsc --noEmit && npx biome check .</automated>
  </verify>
  <acceptance_criteria>
    - `src/keeping/types.ts` exports `KeepingUser` and `KeepingOrg` as `interface` declarations (NOT Zod schemas — D-34 looseness intentional)
    - `src/keeping/errors.ts` exports all six symbols listed in the `<interfaces>` block in `<context>`
    - `new KeepingAuthError().message === "Keeping rejected the token. Verify KEEPING_TOKEN and restart the MCP server."` (byte-identical — single test failure on this string blocks the plan)
    - `new MultiOrgError([{id:'a',name:'A'},{id:'b',name:'B'}]).message === "Multiple organisations available. Pass organisation_id, or set KEEPING_ORG_ID. Options: a (A), b (B)."` (byte-identical)
    - `sanitiseBody("x kp_test_FAKE y", "kp_test_FAKE") === "x *** y"`
    - `test/keeping/errors.test.ts` contains all six test cases described in `<behavior>`
    - `npx vitest run test/keeping/errors.test.ts` exits with code 0 (six tests pass)
    - `npx tsc --noEmit` exits with code 0 (no type errors)
    - `npx biome check .` exits with code 0 (no lint or format issues)
    - Two commits exist: one with the failing tests, one with the implementation that makes them pass (RED then GREEN)
  </acceptance_criteria>
  <done>
    Leaf contracts in tree; tests green; ready for Plan 02-02 to construct `KeepingClient` that
    imports from these modules.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → npm registry | New deps come from npm; supply-chain risk if any package is typo-squatted or has malicious postinstall. |
| Error message → tool response → MCP client | An API error body may contain the bearer token; error classes must scrub it BEFORE the logger sees it. |
| User → `.gitignore` rules | Raw API capture in Plan 02-05 must never reach git; ignore rules are the gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-SC | Tampering | `@modelcontextprotocol/sdk`, `p-throttle`, `p-retry`, `tsx` npm installs | mitigate | Task 1 = blocking-human checkpoint; user visually verifies each package's npm page + GitHub repo + maintainer before install. Per RESEARCH §Package Legitimacy Audit, slopcheck unavailable so this is the fallback. Cannot be auto-approved. |
| T-02-01-02 | Information Disclosure | `KeepingApiError` constructor capturing Keeping API error body | mitigate | `sanitiseBody(text, token)` runs `replaceAll(token, "***")` before storing on the error (PATTERNS line 333; Pitfall G). Test 4 asserts the token never reaches the error message. Defence-in-depth with logger emit-time redaction (D-08). |
| T-02-01-03 | Information Disclosure | Raw probe capture file at `.planning/research/.live-capture-raw.json` | mitigate | `.gitignore` augmented in Task 2 BEFORE Plan 02-05 creates the script that writes the raw file. Cannot commit raw capture even by accident. |
| T-02-01-04 | Information Disclosure | `KeepingAuthError` message echoes status of the (now invalid) token attempt | accept | The message intentionally does NOT echo the token; it asks the user to verify and restart. No PII, no secret value embedded — only the variable name `KEEPING_TOKEN`. Reviewed and acceptable per D-25. |

</threat_model>

<verification>
After completion, all three of these must hold simultaneously:

1. `npx vitest run` passes ALL tests (Phase 1 logger + new Phase 2 errors).
2. `npx tsc --noEmit` passes with zero errors.
3. `npx biome check .` passes with zero violations.

Manual spot-check: open `.gitignore` and confirm the two new lines appear at the bottom; open
`package.json` and confirm the four new dep entries.
</verification>

<success_criteria>
- All three required leaf modules created with the exact export surface declared in `<interfaces>`
- The two byte-identical D-25 / D-27 strings are PRESENT in `src/keeping/errors.ts` exactly as specified (the tests are the gate)
- Dependencies installed at the RESEARCH-pinned versions
- `.gitignore` includes both D-37 lines
- No production code references `src/keeping/client.ts` or `src/server.ts` yet (those files do not exist in this plan)
- Two atomic commits demonstrate the RED→GREEN cycle for the errors module
</success_criteria>

<output>
Create `.planning/phases/02-read-tools-schema-discovery/02-01-SUMMARY.md` when done. Record:
- Exact versions resolved by npm (e.g., `@modelcontextprotocol/sdk@1.29.0`, etc.)
- Whether any package looked suspicious during the slopcheck-fallback verify
- Confirmation that both D-25 and D-27 strings pass exact-equality tests
- Any deviation from the planned export surface (none expected)
</output>
