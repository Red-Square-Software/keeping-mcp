---
phase: 02-read-tools-schema-discovery
plan: 05
type: execute
wave: 5
depends_on:
  - 02-04
files_modified:
  - package.json
  - scripts/probe-live.ts
  - test/scripts/anonymise.test.ts
autonomous: true
requirements:
  - READ-02
user_setup:
  - service: keeping-api
    why: "The probe script needs a real KEEPING_TOKEN to capture timer endpoint state + a real time-entry response. The script itself is shipped in this plan; the human running it (Plan 02-06) is the gate."
    env_vars:
      - name: KEEPING_TOKEN
        source: "Keeping Settings → Preferences → enable developer features → generate personal access token"
      - name: KEEPING_ORG_ID
        source: "Optional. From keeping_organisations output, or browser URL when logged into Keeping."
      - name: PROBE_FROM
        source: "Optional. YYYY-MM-DD start date for time-entry capture. Defaults to 7 days ago."
      - name: PROBE_TO
        source: "Optional. YYYY-MM-DD end date. Defaults to PROBE_FROM."

must_haves:
  truths:
    - "Decisions implemented in this plan: D-30, D-31, D-32, D-35"
    - "`scripts/probe-live.ts` exists and can be invoked via `npm run probe-live` (uses tsx + --env-file-if-exists per Pitfall D + H)"
    - "Script fails fast with stderr message `[probe-live] KEEPING_TOKEN must be set in env or .env before running probe-live` and exit code 1 when KEEPING_TOKEN is unset (fail-fast carry-forward from D-05 pattern)"
    - "Script probes three timer endpoint paths in parallel via Promise.allSettled (D-31): `/organisations/:id/timers`, `/organisations/:id/timers/current`, `/organisations/:id/time_entries?running=true`"
    - "Script also probes `/v1/users/me` (single GET) to resolve RESEARCH Q1 (path commitment for KeepingClient.me()). Status code (200 / 404 / other) is recorded in LIVE-API.md `## /v1/users/me path probe` so Plan 02-06 Task 3 can decide whether to apply the contingency code fix."
    - "Script captures one `time_entries` response for a user-supplied (or default 7-day) date range and writes the raw JSON to `.planning/research/.live-capture-raw.json` (gitignored per D-37) — IF PROBE_FROM is set OR after the user confirms the default range is acceptable"
    - "Script applies an `anonymise()` walker that replaces values at the keys `description`, `project_name`, `task_name`, `client_name`, `user_name`, `user_email` with `'[REDACTED]'` (D-35 step 3 + CONTEXT.md §Specific Ideas line 148)"
    - "Script writes the anonymised result to `test/fixtures/time-entry-response.sample.json` for commit (D-35 step 4)"
    - "Script writes a structured human-notes file to `.planning/research/LIVE-API.md` with the six mandated sections (CONTEXT.md §Specific Ideas line 149 + the new /v1/users/me path probe section added per RESEARCH Q1 contingency): 'Timer endpoint result', '/v1/users/me path probe', 'Time entry response shape', 'Observed enum values (purpose, timesheet_mode)', 'Pagination scheme observed', 'Error envelope observed'"
    - "All script output goes to stderr only (`console.error` allowed by biome, never `console.log`); user-facing progress messages prefixed `[probe-live]` to match the project log convention"
    - "Anonymise unit test asserts the six denylist keys are scrubbed at any nesting depth (array-of-objects, nested objects, top-level) and that IDs / timestamps / numeric fields pass through unchanged"
    - "`package.json` declares `\"probe-live\": \"tsx --env-file-if-exists=.env scripts/probe-live.ts\"` (Pitfall D + H)"
  artifacts:
    - path: "scripts/probe-live.ts"
      provides: "One-shot live API probe — captures timer endpoint state + anonymised time-entry fixture + LIVE-API.md notes"
      min_lines: 100
      contains: "Promise.allSettled"
    - path: "test/scripts/anonymise.test.ts"
      provides: "Unit tests for the anonymise() walker — denylist coverage at every nesting depth"
      min_lines: 40
    - path: "package.json"
      provides: "New `probe-live` script entry"
      contains: "probe-live"
  key_links:
    - from: "scripts/probe-live.ts"
      to: "src/keeping/client.ts KeepingClient"
      via: "Reuses the same client for auth, throttle, retry, sanitisation; no parallel fetch implementation"
      pattern: "new KeepingClient"
    - from: "scripts/probe-live.ts"
      to: "src/config.ts loadConfig"
      via: "Uses the same fail-fast config validation as the MCP server entrypoint (D-04, D-05 carry-forward)"
      pattern: "loadConfig\\("
    - from: ".gitignore (modified in 02-01)"
      to: "scripts/probe-live.ts raw capture output"
      via: "Raw capture path `.planning/research/.live-capture-raw.json` matches a .gitignore entry"
      pattern: "live-capture-raw"
    - from: "scripts/probe-live.ts"
      to: "test/fixtures/time-entry-response.sample.json"
      via: "Written by step 4 of the script's main flow"
      pattern: "time-entry-response\\.sample\\.json"
---

<objective>
Wave 5 of Phase 2: ship the **one-shot live probe** that captures timer-endpoint reality (D-31)
and an anonymised time-entry fixture (D-35) for Phase 3 consumption. The script never runs in
production code paths — it is invoked manually by the developer with a real `KEEPING_TOKEN`. The
script in this plan and its running (Plan 02-06) are deliberately separated: this plan ships the
code; Plan 02-06 is the blocking human-verify checkpoint that runs it.

The probe satisfies D-30 (one-shot npm script, not server-startup), D-31 (three timer paths in
parallel), D-32 (writes LIVE-API.md + flags REQUIREMENTS update for Plan 02-06), D-35 (folds
time-entry capture and anonymisation into the same script).

Purpose: deliver the script + tested anonymiser + npm script entry. After this plan, the
artefact required for Plan 02-06's checkpoint is in place; the user can run
`KEEPING_TOKEN=... npm run probe-live` and get back human-readable session notes + a committable
fixture + REQUIREMENTS-update guidance.
Output: `scripts/probe-live.ts`, `test/scripts/anonymise.test.ts`, npm script in `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-read-tools-schema-discovery/02-CONTEXT.md
@.planning/phases/02-read-tools-schema-discovery/02-RESEARCH.md
@.planning/phases/02-read-tools-schema-discovery/02-PATTERNS.md
@.planning/phases/02-read-tools-schema-discovery/02-04-PLAN.md
@src/keeping/client.ts
@src/keeping/errors.ts
@src/config.ts
@src/logger.ts
@bin/keeping-mcp.ts
@package.json
@.gitignore

<interfaces>
<!-- KeepingClient surface (from Plan 02-02) — the probe REUSES this -->

```typescript
export class KeepingClient {
  constructor(token: string, log: Logger);
  resolveOrgId(input?: string): Promise<string>;
  get<T>(path: string): Promise<T>;
  organisations(): Promise<KeepingOrg[]>;
}
```

<!-- Anonymise utility this plan defines + tests -->

```typescript
const ANONYMISE_KEYS = new Set([
  "description",
  "project_name",
  "task_name",
  "client_name",
  "user_name",
  "user_email",
]);

function anonymise(value: unknown): unknown;
// At keys in ANONYMISE_KEYS: replace value with "[REDACTED]"
// At all other keys / array elements: recurse
// Strings/numbers/booleans/null at leaf positions (NOT at a denylisted key): pass through
```

<!-- Script flow (RESEARCH §"Common Operation 6" lines 953-1064) -->

1. Load config (loadConfig() — exits on missing KEEPING_TOKEN with the project-standard fail-fast).
2. Construct logger + KeepingClient.
3. Resolve org id (uses KEEPING_ORG_ID env or single-org auto-detect or fails with MultiOrgError).
4. Probe THREE timer paths in parallel via `Promise.allSettled` (D-31):
   - `/organisations/${orgId}/timers`
   - `/organisations/${orgId}/timers/current`
   - `/organisations/${orgId}/time_entries?running=true`
   Each result is `{ path, ok: true, body }` on success or `{ path, ok: false, error: <message>, status?: <number> }` on failure. CATCH errors per-promise so one failure doesn't kill the others.
5. Capture one time-entry response for a date range. Default range: last 7 days. Override via
   PROBE_FROM / PROBE_TO env vars.
6. Write raw JSON to `.planning/research/.live-capture-raw.json` (gitignored).
7. Write anonymised JSON to `test/fixtures/time-entry-response.sample.json` (committed).
8. Write structured LIVE-API.md to `.planning/research/LIVE-API.md` (committed). Sections required
   per CONTEXT §Specific Ideas line 149:
   - `# Live API Capture` (top-level heading)
   - `## Timer endpoint result` — which of the three paths returned 200 vs 404; preview of body when 200.
   - `## /v1/users/me path probe` — the status code returned by `GET /v1/users/me` (200, 404, or other). This section drives Plan 02-06 Task 3's Q1 path contingency decision (RESEARCH §Open Questions RESOLVED, Q1).
   - `## Time entry response shape` — observed top-level keys (array vs `{entries: [...]}` wrapper); for the first entry, list every key + sample value type.
   - `## Observed enum values (purpose, timesheet_mode)` — distinct values seen across entries + the org's `timesheet_mode` field.
   - `## Pagination scheme observed` — `meta.total`, `next_cursor`, `Link` header — whatever was present.
   - `## Error envelope observed` — if any of the three timer probes returned non-404 errors, document the body shape.
   - `## REQUIREMENTS update for Phase 3` — a literal block telling the human running Plan 02-06 what to update in REQUIREMENTS.md for TIMER-01, e.g. `Update TIMER-01 row: "verified — endpoint <path> — ships in Phase 3"` or `"deferred — 404 on all probes — dropped from v1"`.

User-facing progress lines via `console.error` (NEVER `console.log` — Pitfall F + biome `noConsole`):
- `[probe-live] probing against organisation_id=<id>`
- `[probe-live] timer probe 1/3: <path> -> <status or error>` for each path
- `[probe-live] /v1/users/me path probe -> <status>` (Q1 contingency evidence)
- `[probe-live] capturing time_entries from=<from> to=<to>`
- `[probe-live] raw capture: .planning/research/.live-capture-raw.json` (so the user can sanity-check it before commit)
- `[probe-live] anonymised fixture: test/fixtures/time-entry-response.sample.json`
- `[probe-live] human notes: .planning/research/LIVE-API.md`
- `[probe-live] probe-live complete. Review LIVE-API.md and commit the fixture + notes after confirming the anonymised output contains no PII.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement and test the `anonymise()` walker</name>
  <read_first>
    - .planning/phases/02-read-tools-schema-discovery/02-RESEARCH.md §"Pattern 4: Anonymisation utility" (lines 524-552) — full target implementation
    - .planning/phases/02-read-tools-schema-discovery/02-CONTEXT.md §"Specific Ideas" line 148 — exact denylist field names
    - test/logger.test.ts (vitest style)
  </read_first>
  <behavior>
    `test/scripts/anonymise.test.ts` imports `anonymise` and `ANONYMISE_KEYS` from
    `../../scripts/probe-live.ts` (the script must export them for testability).

    - Test 1: Top-level denylisted key — `anonymise({ description: "client meeting" })` returns
      `{ description: "[REDACTED]" }`.
    - Test 2: Top-level non-denylisted key — `anonymise({ id: "te-1", hours: 1.5 })` returns
      `{ id: "te-1", hours: 1.5 }` (verbatim).
    - Test 3: Nested object — `anonymise({ user: { user_email: "x@y.z", id: "u-1" } })` returns
      `{ user: { user_email: "[REDACTED]", id: "u-1" } }`.
    - Test 4: Array of objects — `anonymise([{ description: "a", id: "1" }, { description: "b", id: "2" }])`
      returns `[{ description: "[REDACTED]", id: "1" }, { description: "[REDACTED]", id: "2" }]`.
    - Test 5: All six denylist keys — assert each of `description`, `project_name`, `task_name`,
      `client_name`, `user_name`, `user_email` is replaced when present at the top level.
    - Test 6: Null + boolean + number preservation — `anonymise({ hours: 0, billable: true, deleted: null })`
      returns the same object.
    - Test 7: Empty array / empty object — `anonymise([])` returns `[]`; `anonymise({})` returns `{}`.
    - Test 8: Primitive at root — `anonymise("hello")` returns `"hello"`; `anonymise(42)` returns `42`; `anonymise(null)` returns `null`.
    - Test 9: ANONYMISE_KEYS exact set — assert `ANONYMISE_KEYS.size === 6` and contains each of the six names exactly once. (Regression guard — if someone adds a key without updating CONTEXT.md, the test should fail and force them to revisit.)
  </behavior>
  <action>
    RED step: create `test/scripts/anonymise.test.ts` with the 9 tests above. Run vitest — all
    fail (script doesn't exist). Commit: `test(02-05): add failing tests for anonymise walker`.

    GREEN step: create `scripts/probe-live.ts` with ONLY the anonymise primitive + denylist
    constant exported (the full script body comes in Task 2). Per RESEARCH lines 532-551:
    ```
    export const ANONYMISE_KEYS = new Set([
      "description", "project_name", "task_name",
      "client_name", "user_name", "user_email",
    ]);

    export function anonymise(value: unknown): unknown {
      if (Array.isArray(value)) return value.map(anonymise);
      if (value !== null && typeof value === "object") {
        const out: Record<string, unknown> = {};
        for (const [k, v] of Object.entries(value as Record<string, unknown>)) {
          out[k] = ANONYMISE_KEYS.has(k) ? "[REDACTED]" : anonymise(v);
        }
        return out;
      }
      return value;
    }
    ```

    Stub the rest of the script with `// TODO Task 2: main flow` — vitest only imports the
    exports, so the file does not need to be executable yet. Run vitest — all 9 must PASS.
    Commit: `feat(02-05): add anonymise walker for probe-live`.

    REFACTOR: `npx biome check .` + `npx tsc --noEmit`. (tsx is dev-only — `tsc --noEmit` still
    typechecks the file as long as it parses.)
  </action>
  <verify>
    <automated>npx vitest run test/scripts/anonymise.test.ts && npx tsc --noEmit && npx biome check .</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/probe-live.ts` exists and exports `anonymise` + `ANONYMISE_KEYS`
    - 9/9 anonymise tests pass
    - `ANONYMISE_KEYS` size is exactly 6 (CONTEXT lock — no scope creep)
    - Walker handles nested arrays, nested objects, primitives, and null at every depth
    - `npx tsc --noEmit` and `npx biome check .` exit 0
    - RED→GREEN commits exist
  </acceptance_criteria>
  <done>
    Anonymiser shipped and tested. Task 2 wraps the live HTTP flow around it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement the probe script's main flow + register `npm run probe-live`</name>
  <read_first>
    - scripts/probe-live.ts (after Task 1 — extend the existing file; preserve the anonymise + ANONYMISE_KEYS exports)
    - src/keeping/client.ts (constructor signature, public methods used by the probe)
    - src/config.ts (loadConfig signature + fail-fast behaviour)
    - src/logger.ts (createLogger signature)
    - bin/keeping-mcp.ts (the existing entrypoint — model the probe's startup wiring after this file)
    - .planning/phases/02-read-tools-schema-discovery/02-RESEARCH.md §"Common Operation 5" (lines 940-949) — package.json script entry
    - .planning/phases/02-read-tools-schema-discovery/02-RESEARCH.md §"Common Operation 6" (lines 953-1064) — full script skeleton
    - .planning/phases/02-read-tools-schema-discovery/02-CONTEXT.md §"Specific Ideas" line 149 — required LIVE-API.md sections
    - .planning/phases/02-read-tools-schema-discovery/02-RESEARCH.md §"Pitfall D" + "Pitfall H" (lines 674-684, 727-735)
    - package.json (current — preserve all existing scripts; append the new one)
  </read_first>
  <action>
    Extend `scripts/probe-live.ts` with the main flow. Per RESEARCH lines 953-1064 with the
    extensions below. Final file structure (top to bottom):

    1. Imports: `import { loadConfig } from "../src/config.js";`, `import { createLogger } from "../src/logger.js";`, `import { KeepingClient } from "../src/keeping/client.js";`, `import { writeFile, mkdir } from "node:fs/promises";`, `import { dirname } from "node:path";`.

    2. Exported `ANONYMISE_KEYS` + `anonymise` (already shipped by Task 1 — keep them).

    3. `writeJson(path, value)` helper that mkdir's the parent dir then writes pretty-printed JSON.

    4. `defaultLastWeek()` helper — returns `new Date()` minus 7 days as YYYY-MM-DD. Per Pitfall 5
       use calendar-date string formatting (do NOT use `toISOString().slice(0,10)` if the test machine
       crosses UTC boundary — use `.getFullYear()/.getMonth()/.getDate()` for Europe/Amsterdam-safe
       behaviour). Acceptable: `toISOString().slice(0,10)` is fine here because this is a one-shot
       script run by the developer, not a server boundary — note this with a comment.

    5. `main()` async function with this exact sequence:

       a. `const config = loadConfig();` — fails fast with the project-standard stderr message if
          KEEPING_TOKEN is unset. NO need for a separate "[probe-live] KEEPING_TOKEN must be set"
          message — `loadConfig()` already emits the verbatim D-05 wording. BUT: the
          `<must_haves>` truth specifies a probe-specific fail-fast message. Resolve this by adding
          a pre-check IN THE SCRIPT before `loadConfig()`:
          ```
          if (!process.env.KEEPING_TOKEN) {
            process.stderr.write("[probe-live] KEEPING_TOKEN must be set in env or .env before running probe-live\n");
            process.exit(1);
          }
          ```
          Then call `loadConfig()` as a normal validator for the rest of the env. Both messages
          may appear in some edge cases — that's fine; the probe-specific one fires first and is
          the user's primary cue.

       b. `const log = createLogger(config.KEEPING_TOKEN, config.KEEPING_LOG_LEVEL);`

       c. `const client = new KeepingClient(config.KEEPING_TOKEN, log);`

       d. `const orgId = await client.resolveOrgId();` — uses KEEPING_ORG_ID env or single-org
          auto-detect. If multi-org with no env, the `MultiOrgError` propagates and main()'s
          catch handler prints it + exits 1.

       e. `console.error(\`[probe-live] probing against organisation_id=${orgId}\`);`

       f. Build `TIMER_PATHS` array per RESEARCH lines 1004-1008. Use `Promise.allSettled` to fire
          all three in parallel. For each result, capture `{ path, ok, body | error, status }`.
          Log progress: `console.error(\`[probe-live] timer probe ${i+1}/3: ${path} -> ${result}\`)`.

       f-bis. **/v1/users/me path probe (RESEARCH Q1 contingency evidence)**: AFTER the three
          timer probes resolve, run a single `fetch` (NOT via `client.me()` — that would 401 the
          whole probe early; we want the raw status code). Construct:
          ```
          const meRes = await fetch("https://api.keeping.nl/v1/users/me", {
            method: "GET",
            headers: { Authorization: \`Bearer ${config.KEEPING_TOKEN}\`, Accept: "application/json" },
            signal: AbortSignal.timeout(10_000),
          });
          const meProbe = { path: "/v1/users/me", ok: meRes.ok, status: meRes.status };
          console.error(\`[probe-live] /v1/users/me path probe -> ${meRes.status}\`);
          ```
          Capture `meProbe` for use in step 6 (raw capture) + step k (LIVE-API.md `## /v1/users/me path probe` section). This is the evidence Plan 02-06 Task 3 reads to decide whether to switch KeepingClient.me() to the org-scoped path.

       g. Determine date range: `const from = process.env.PROBE_FROM ?? defaultLastWeek(); const to = process.env.PROBE_TO ?? from;`

       h. `console.error(\`[probe-live] capturing time_entries from=${from} to=${to}\`);` and call
          `await client.get(\`/organisations/${orgId}/time_entries?from=${from}&to=${to}\`)`.
          If this throws, log + exit 1.

       i. Write the raw capture:
          ```
          await writeJson(".planning/research/.live-capture-raw.json", {
            captured_at: new Date().toISOString(),
            organisation_id: orgId,
            timers: probes,
            users_me_probe: meProbe,
            time_entries: { from, to, body: entries },
          });
          ```
          (Per D-37, this file is gitignored. The script writes it anyway — useful for the
          developer to inspect before commit.)

       j. Anonymise + write the fixture:
          ```
          await writeJson("test/fixtures/time-entry-response.sample.json", anonymise(entries));
          ```

       k. Build `LIVE-API.md` content via a helper `buildLiveApiNotes(probes, meProbe, entries, from, to)`.
          The helper produces a string with the literal section headings listed in `<interfaces>`.
          For each section, populate from the captured data:

          - **Timer endpoint result**: enumerate each path with its status / error. Mark the
            "winner" (first 200 response) with `**WINNING PATH**: <path>`. If all three 404'd,
            write `All three timer paths returned 404. TIMER-01 is deferred from v1 per D-32.`
          - **/v1/users/me path probe**: write `Status: ${meProbe.status}.` If `meProbe.status === 200`, append `Decision: no code change required; KeepingClient.me() stays on /v1/users/me.` If `meProbe.status === 404`, append `Decision: Plan 02-06 Task 3 must switch KeepingClient.me() to the org-scoped form /v1/organisations/${orgId}/users/me (RESEARCH Q1 contingency).` For any other status, append `Decision: unexpected status; Plan 02-06 Task 3 must investigate before committing.`
          - **Time entry response shape**: if `entries` is an array, write `Top-level shape: array of N items.`; if it's `{entries: [...]}`, write `Top-level shape: object with key 'entries' containing N items.`. Then list the first item's keys (`Object.keys(entries[0])`) with the typeof each value.
          - **Observed enum values**: scan entries for the `purpose` field — list distinct values. Scan org list (from `client.organisations()` — call it again, it's cached) for `timesheet_mode` field. If neither found, write `No purpose or timesheet_mode field present in captured response.`
          - **Pagination scheme observed**: look for top-level keys `meta`, `pagination`, `next_cursor`, `links`, etc.; report what was present (or `Not observed in this capture.`).
          - **Error envelope observed**: for any timer probe with `ok: false`, list the path + error message. If all probes succeeded, write `No error envelopes captured in this run.`
          - **REQUIREMENTS update for Phase 3**: a literal copy-paste block instructing the user
            running Plan 02-06 to update the TIMER-01 row in `.planning/REQUIREMENTS.md`. Include
            the exact text to substitute: `TIMER-01 | Phase 3 | <one of: 'verified — endpoint <path>' OR 'deferred — 404 on all probes'>`.

       l. Write LIVE-API.md:
          ```
          await writeFile(".planning/research/LIVE-API.md", buildLiveApiNotes(probes, meProbe, entries, from, to), "utf8");
          ```

       m. Final progress lines per `<interfaces>` section.

    6. `main().catch((err) => { process.stderr.write(\`[probe-live] FAILED: ${(err as Error).message}\n\`); process.exit(1); });`

    Update `package.json`: add `"probe-live": "tsx --env-file-if-exists=.env scripts/probe-live.ts"`
    to the `scripts` object (alphabetised between `lint` and `test` if you order alphabetically,
    or appended at the end — either is fine; existing scripts unchanged).

    Do NOT actually run the script (no real token available; that's Plan 02-06's checkpoint job).
    Verification is structural: file exists, exports correct, `npm run probe-live` would resolve
    the tsx binary if dotenv-less.

    Run anonymise tests again (regression): `npx vitest run test/scripts/anonymise.test.ts` — still
    all 9 pass.

    Commit: `feat(02-05): add probe-live script for timer endpoint + time-entry schema capture`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts['probe-live']) { console.error('MISSING script probe-live'); process.exit(1); } if(!p.scripts['probe-live'].includes('--env-file-if-exists=.env')) { console.error('MISSING --env-file-if-exists flag'); process.exit(1); } console.log('probe-live script OK')" && npx tsc --noEmit && npx biome check . && npx vitest run test/scripts/anonymise.test.ts</automated>
    Additional: `Grep "console\\.log" scripts/` returns zero matches (biome enforces but trust-but-verify); `Grep "Promise\\.allSettled" scripts/probe-live.ts` finds one match (D-31).
  </verify>
  <acceptance_criteria>
    - `scripts/probe-live.ts` contains a `main()` function with the exact sequence in `<action>` step 5
    - Script uses `Promise.allSettled` (NOT `Promise.all`) for the three timer paths (D-31 — one failure must not kill the others)
    - Script issues a single `fetch` to `/v1/users/me` AFTER the timer probes, capturing status into a `meProbe` object that is included in BOTH the raw capture file AND the new `## /v1/users/me path probe` section of LIVE-API.md (RESEARCH Q1 contingency evidence)
    - Script writes RAW capture to `.planning/research/.live-capture-raw.json` (D-37 path, will be ignored by git)
    - Script writes ANONYMISED fixture to `test/fixtures/time-entry-response.sample.json` (D-35)
    - Script writes LIVE-API.md with all seven required section headings: Timer endpoint result, /v1/users/me path probe, Time entry response shape, Observed enum values, Pagination scheme observed, Error envelope observed, REQUIREMENTS update for Phase 3 (CONTEXT line 149 + the path-probe section + the REQUIREMENTS-update block)
    - Script uses `console.error` for every progress line (Pitfall F + biome `noConsole`)
    - Pre-check at top of `main()` emits the byte-identical `[probe-live] KEEPING_TOKEN must be set in env or .env before running probe-live` message and exits 1 when KEEPING_TOKEN is unset
    - `package.json` `scripts.probe-live === "tsx --env-file-if-exists=.env scripts/probe-live.ts"` (exact value — Pitfall D + H)
    - `npx tsc --noEmit` passes (script typechecks)
    - `npx biome check .` passes
    - All previous tests still pass (no regression)
  </acceptance_criteria>
  <done>
    Probe script in tree. Plan 02-06 (human-verify) will execute it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `process.env.KEEPING_TOKEN` (loaded via `--env-file-if-exists` or shell env) → KeepingClient | Real bearer token enters the process; same constraints as the server entrypoint |
| API response body (real client data) → raw capture file → developer's local FS | Raw capture contains real time-entry data (descriptions, project names, etc.) |
| API response body → anonymise() → committed fixture | Anonymisation is the gate that turns private data into a committable artefact |
| LIVE-API.md text → committed to git | Notes summarise the schema and CANNOT include raw values from PII-bearing fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information Disclosure | Raw API capture at `.planning/research/.live-capture-raw.json` committed accidentally | mitigate | `.gitignore` augmented in Plan 02-01 (D-37) with both `.live-capture-raw.json` and `.probe-raw-*.json` patterns. The script writes the raw file at the gitignored path explicitly. Even `git add -A` would skip it. |
| T-02-05-02 | Information Disclosure | Anonymise denylist misses a field (e.g., a new "client_email" key Keeping adds later) | mitigate | (1) `ANONYMISE_KEYS` Set is asserted size === 6 in Test 9 — adding a key without bumping the test forces revisit. (2) Plan 02-06 includes a human-review step where the developer eyeballs the fixture before commit. (3) RESEARCH §"Anonymisation" notes that allow-list was rejected because it would silently drop new fields — by using a denylist we surface them; the developer notices in review. (4) Per CONTEXT discretion, the planner extends the denylist if the probe reveals additional human-named fields — that triggers a follow-up plan. |
| T-02-05-03 | Information Disclosure | `KEEPING_TOKEN` written into a log line by the probe script | mitigate | Logger (`createLogger(token, ...)`) redacts at emit; all `console.error` in the script writes through the logger or direct stderr without the token. Probe script NEVER stringifies the request headers or the KeepingClient instance into a log. |
| T-02-05-04 | Information Disclosure | `LIVE-API.md` includes raw field VALUES (not just field NAMES) — e.g., quoting "description: 'Met with Acme'" | mitigate | `buildLiveApiNotes` lists `Object.keys(entries[0])` + the `typeof` each value — NOT the value itself. Distinct purpose / timesheet_mode enum values are listed, but those are enum codes ("billable", "non_billable", "approval"), not human-named PII. The developer eyeballs LIVE-API.md before commit in Plan 02-06 as a backstop. |
| T-02-05-05 | Tampering | A malicious `.env` file (e.g., copied from a different project) supplies a wrong KEEPING_TOKEN → probe writes a fixture for the wrong account | accept | The developer knowingly invokes the probe; they would notice an unexpected `organisation_id` in the first stderr log line. The fixture is named `time-entry-response.sample.json` (sample, not production) — even if wrong, it's a schema sample for Phase 3, not a data artefact. |
| T-02-05-06 | Information Disclosure | `--env-file-if-exists=.env` reads a file the developer may not realise is in the working directory | accept | Standard Node.js behaviour. The `.env` is in `.gitignore` (carry from Phase 1). The "if-exists" form is friendlier than the bare form (Pitfall D) and matches user expectation. |

</threat_model>

<verification>
After completion:

1. `npx vitest run` — ALL tests pass (anonymise additions on top of prior plans).
2. `npx tsc --noEmit` — zero errors.
3. `npx biome check .` — zero violations.
4. `npm run build` — succeeds.
5. `node -e "require('./package.json').scripts['probe-live']"` returns the exact tsx command.
6. Manual smoke: `npm run probe-live` invoked with NO `KEEPING_TOKEN` env emits the byte-identical `[probe-live] KEEPING_TOKEN must be set in env or .env before running probe-live` message and exits 1. (No real API call attempted — script aborts at the pre-check.)
</verification>

<success_criteria>
- The probe-live script is in tree and ready to run against a real KEEPING_TOKEN
- The anonymise() walker provably handles every nesting case (9 tests passing)
- `npm run probe-live` is wired in package.json with the tolerant `--env-file-if-exists` flag (Pitfall D)
- The probe-live failure-mode without a token is friendly and explicit (probe-specific stderr message, not just the generic Configuration error)
- No production code path (bin/, src/) is touched by this plan — strict isolation
</success_criteria>

<output>
Create `.planning/phases/02-read-tools-schema-discovery/02-05-SUMMARY.md` when done. Record:
- 9/9 anonymise tests passing
- Manual fail-fast smoke output (the byte-identical KEEPING_TOKEN-missing stderr line)
- Confirmation that no production code under bin/ or src/ was modified
- Final `scripts/probe-live.ts` line count
</output>
