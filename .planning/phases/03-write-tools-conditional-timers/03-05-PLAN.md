---
phase: 03-write-tools-conditional-timers
plan: 05
type: tdd
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/tools/start-timer.ts
  - test/tools/start-timer.test.ts
autonomous: true
requirements:
  - TIMER-01
  - WRITE-04
  - WRITE-05
  - WRITE-07
tags:
  - keeping-mcp
  - phase-3
  - write-tools
  - timer-tools
  - mcp-server
  - start-timer

must_haves:
  truths:
    - "MCP client calling keeping_start_timer with confirm: true POSTs to /{orgId}/time-entries with a body that includes `start` (defaults to nowInAmsterdamHHMM()) and explicitly OMITS both `end` and `hours` keys (D-3-06, D-3-24)"
    - "Tool extracts the ongoing entry's id from `response.time_entry.id` via the verbatim three-clause Array.isArray guard (D-2.5-05a) and returns `{ timer_id: <number> }` on the confirm path"
    - "Dry-run preview returns `would_post.body` with start set + no end + no hours"
    - "Dry-run preview does NOT include `timer_id` in its response (there is no entry yet to extract from)"
    - "DST default: with Date.now() = 2026-06-12T22:30:00Z, would_post.body.date === '2026-06-13' AND would_post.body.start === '00:30' (D-3-26, D-3-28)"
    - "purpose defaults to 'work'; project_id, task_id, note all optional"
    - "Annotations: D-3-11 four booleans"
    - "5xx → ambiguous; 4xx → toIsErrorContent"
  artifacts:
    - path: "src/tools/start-timer.ts"
      provides: "registerStartTimer(server, client, config) for keeping_start_timer"
      contains: "export function registerStartTimer"
      contains_also: "extractTimeEntry"
      contains_third: "timer_id"
    - path: "test/tools/start-timer.test.ts"
      provides: "Per-tool test set + strict Object.keys assertion on posted body (D-3-24)"
      min_lines: 200
  key_links:
    - from: "src/tools/start-timer.ts"
      to: "POST /{orgId}/time-entries with no end/no hours"
      via: "previewOrCall({ method: 'POST', path: '/${orgId}/time-entries', body })"
      pattern: "method: \"POST\""
    - from: "src/tools/start-timer.ts"
      to: "strict wrapper extractor"
      via: "verbatim three-clause guard from src/tools/timer-status.ts:58-65"
      pattern: "Array\\.isArray"
---

<objective>
Ship `keeping_start_timer`. Implements the "no dedicated /start endpoint" rule from D-3-06: start = POST `/{orgId}/time-entries` with `start` set (defaults to `nowInAmsterdamHHMM()` per D-3-28) and BOTH `end` and `hours` omitted from the body. Body's omission of `end` is what Keeping interprets as "ongoing entry". On the confirm path, extract `time_entry.id` via the verbatim three-clause `Array.isArray` guard from D-2.5-05a and return `{ timer_id }`.

Purpose: TIMER-01 (start portion). Also exercises WRITE-04 gate + WRITE-05 ambiguous-failure + WRITE-07 annotations.

Output: One new tool file + one new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-write-tools-conditional-timers/03-CONTEXT.md
@.planning/phases/03-write-tools-conditional-timers/03-RESEARCH.md
@.planning/phases/03-write-tools-conditional-timers/03-PATTERNS.md
@.planning/phases/03-write-tools-conditional-timers/03-01-SUMMARY.md
@.planning/phases/02.5-timer-status-read-tool/02.5-CONTEXT.md

# Foundation
@src/keeping/write-gate.ts
@src/keeping/date.ts
@src/keeping/client.ts
@src/keeping/types.ts

# Canonical strict-wrapper-read precedent
@src/tools/timer-status.ts
@test/tools/timer-status.test.ts

<interfaces>
- `previewOrCall<{ time_entry: unknown; meta?: unknown }>(...)` for the gate
- `todayInAmsterdam`, `nowInAmsterdamHHMM`
- Three-clause guard pattern from `src/tools/timer-status.ts:58-65` — copy verbatim
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → registerStartTimer handler | Same Zod guards as add-entry: numeric project_id/task_id, enum purpose. No entry_id input. |
| Tool → POST /time-entries with no end | Keeping interprets missing `end` as "ongoing entry". The body construction MUST NOT include an `end` key (not `end: null`, not `end: undefined` — the key must be absent). Strict Object.keys assertion in Test 4 protects this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | `end`/`hours` accidentally included in start-timer body | mitigate | Body builder only sets `start` (plus optional supplied fields); never touches `end` or `hours`. Test 4 strict `Object.keys(body)` assertion catches a regression. |
| T-03-05-02 | Information Disclosure | Strict wrapper extractor masking drift | mitigate | Three-clause `Array.isArray` guard from D-2.5-05a copied verbatim. If Keeping ever returns `{ time_entry: [] }` (drift), extractor collapses to null and tool returns `{ timer_id: null }` — drift surfaces visibly rather than crashing or pretending success. |
| T-03-05-03 | Tampering | Same AND-gate inversion | mitigate | Same `previewOrCall(client, { requireConfirm, confirm }, ...)` shape. Tests 1 + 3 cover both env-true/false branches. |
| T-03-05-SC | Tampering | npm installs | accept | No installs. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — test/tools/start-timer.test.ts including strict Object.keys body assertion (D-3-24)</name>
  <files>test/tools/start-timer.test.ts</files>

  <read_first>
    - test/tools/timer-status.test.ts (canonical skeleton)
    - 03-CONTEXT.md §D-3-06 (start = POST with no end / no hours), D-3-09 (start_timer input), D-3-22 + D-3-24 (test set + strict Object.keys), D-3-28 (HH:mm)
    - 03-PATTERNS.md §src/tools/start-timer.ts (unique elements + strict wrapper extraction)
    - 02.5-CONTEXT.md §D-2.5-05a (Array.isArray guard) — reused verbatim in this tool
  </read_first>

  <behavior>
    test/tools/start-timer.test.ts (~9 tests). Same buildClient + mockOrgTimes + defaultConfig pattern.

    1. **Test 1: Dry-run preview, no end / no hours in body.**
       Mock: `resolveOrgId → "47666"`, `organisations → [mockOrgTimes]`, `post → throw`. Call with `{ purpose: "work", project_id: 555, start: "14:00" }` (no confirm). Parse `would_post.body`. Assert `Object.keys(body).sort()` equals `["date","project_id","purpose","start"]` (lexicographic). Specifically assert `body.end === undefined` AND `body.hours === undefined` AND `"end" in body === false` AND `"hours" in body === false`.

    2. **Test 2: Confirm path POSTs, response → `{ timer_id }`.**
       Track posts. Mock `post` returns `{ time_entry: { id: 456789123, ongoing: true, ... }, meta: {} }`. Call with same args + `confirm: true`. Assert `posts.length === 1`, `posts[0].path === "/47666/time-entries"`. Strict body Object.keys check (no `end`, no `hours`). Parsed response equals `{ timer_id: 456789123 }`.

    3. **Test 3: Env-false escape** — `KEEPING_REQUIRE_CONFIRM: false` + no confirm → post called, timer_id surfaced.

    4. **Test 4: Default `start` is `nowInAmsterdamHHMM()` when omitted.**
       Use `vi.setSystemTime(new Date("2026-06-12T22:30:00Z"))` for this test (`afterEach` restores). Mock as Test 1. Call with `{ purpose: "work" }` (no `start`). Assert `would_post.body.start === "00:30"`, `would_post.body.date === "2026-06-13"` (DST rollover combined; D-3-26 + D-3-28).

    5. **Test 5: Strict-wrapper drift — `{ time_entry: [] }` collapses to `{ timer_id: null }`.**
       Mock `post` returns `{ time_entry: [], meta: {} }` (an array instead of object — drift). Call with `confirm: true`. Assert `res.isError` falsy AND parsed response equals `{ timer_id: null }`. This is the D-2.5-05a Array.isArray guard in action; drift is visible, not crashing.

    6. **Test 6: Strict-wrapper drift — `{ time_entry: null }` collapses to `{ timer_id: null }`.**
       Similar to Test 5 but `time_entry: null`. Same expected output.

    7. **Test 7: MultiOrgError verbatim.**

    8. **Test 8: 5xx ambiguous envelope.**

    9. **Test 9: Annotation assertion** — D-3-11 four booleans for `keeping_start_timer`.

    All RED.
  </behavior>

  <action>
    Create `test/tools/start-timer.test.ts`. Import `registerStartTimer` from `../../src/tools/start-timer.js` (not yet existing).

    Use the same buildClient + constants pattern. Implement 9 tests above.

    For Test 4 DST, use `vi.useFakeTimers()` + `vi.setSystemTime(new Date("2026-06-12T22:30:00Z"))` in `beforeEach` of that single test (or `it` body) and `vi.useRealTimers()` in `afterEach`. The handler will call `nowInAmsterdamHHMM(new Date())` which honors the faked system time.

    Run vitest; expect RED.

    Commit: `test(03-05): add keeping_start_timer tests — strict no-end/no-hours body, timer_id extraction, drift guard, DST default (TIMER-01, D-3-06, D-3-24)`.
  </action>

  <verify>
    <automated>npx vitest run test/tools/start-timer.test.ts 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - File `test/tools/start-timer.test.ts` exists with at least 9 `it(` declarations.
    - Contains literal `"keeping_start_timer"`, `"timer_id"`, `"Object.keys"`, the strict `expect(body.end).toBeUndefined()` or equivalent assertion, and `"00:30"`.
    - vitest exits non-zero (RED).
    - No `src/tools/start-timer.ts` exists yet.
    - `git log -1 --format=%s` starts with `test(03-05):`.
  </acceptance_criteria>

  <done>RED phase: start-timer contract pinned — strict Object.keys gate, DST default, three-clause guard for timer_id extraction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement src/tools/start-timer.ts</name>
  <files>src/tools/start-timer.ts</files>

  <read_first>
    - 03-PATTERNS.md §src/tools/start-timer.ts (unique elements section + verbatim three-clause guard)
    - 03-CONTEXT.md §D-3-06, D-3-09 (start_timer input fields), D-3-11, D-3-12, D-3-28
    - src/tools/timer-status.ts:58-65 (verbatim source for the `extractTimeEntry` three-clause guard)
    - src/tools/add-entry.ts (sibling — same imports + same catch arm + same purpose enum)
    - The failing tests
  </read_first>

  <action>
    Create `src/tools/start-timer.ts`. Header comment cites `TIMER-01, D-3-06, D-3-09, D-3-11, D-3-12, D-3-16, D-3-28` and the three-clause guard ancestry `D-2.5-05a`.

    Imports: same as add-entry minus we don't strictly need `todayInAmsterdam` (we DO need it because the body needs `date`; if you'd rather not include `date`, the API will default — but D-3-09 implies date defaulting is part of the start body too; the cleanest path is to include `date: input.date ?? todayInAmsterdam()`). Keep both `todayInAmsterdam` AND `nowInAmsterdamHHMM` imports.

    Zod schema `StartTimerInput` (per D-3-09):
    - `organisation_id: z.string().optional().describe("Override KEEPING_ORG_ID; required for multi-org tokens.")`
    - `project_id: z.number().int().positive().optional()`
    - `task_id: z.number().int().positive().optional()`
    - `note: z.string().max(10000).optional()`
    - `purpose: z.enum([...8 values...]).default("work")` (same enum as add-entry — copy literal)
    - `date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional().describe("Calendar date YYYY-MM-DD; defaults to today in Europe/Amsterdam.")`
    - `start: z.string().regex(/^\d{1,2}:\d{2}(:\d{2})?(am|pm)?$/i).optional().describe("HH:mm in org timezone; defaults to the current time in Europe/Amsterdam.")`
    - `confirm: z.boolean().optional().describe(...D-3-12 verbatim...)`

    Note: NO `end`, NO `hours`, NO `tag_ids`, NO `external_references` (D-3-09 explicitly lists `start_timer` input as `{ organisation_id?, project_id?, task_id?, note?, purpose?, start?, confirm?: boolean }`). Keep schema lean.

    Copy `extractTimeEntry` function verbatim from `src/tools/timer-status.ts` lines 58-65 (the three-clause guard). Place it as a module-private helper near the top of the file.

    `registerStartTimer(server, client, config)`:
    - Tool name: `"keeping_start_timer"`
    - Title: `"Start a running timer"`
    - Description (planner draft): `"Start a new ongoing time entry (a running timer). Implemented as POST /{orgId}/time-entries with start set and NO end / NO hours — Keeping interprets the omission of end as 'this entry is ongoing'. Returns { timer_id } on success; use this id with keeping_stop_timer or keeping_resume_timer. DRY-RUN BY DEFAULT — call without confirm first to receive a would_post preview."`
    - inputSchema: `StartTimerInput`
    - annotations: D-3-11 four booleans

    Handler body:
    ```
    async (input) => {
      try {
        const orgId = await client.resolveOrgId(input.organisation_id);
        const body: Record<string, unknown> = {
          date: input.date ?? todayInAmsterdam(),
          purpose: input.purpose,
          start: input.start ?? nowInAmsterdamHHMM(),
        };
        if (input.project_id !== undefined) body.project_id = input.project_id;
        if (input.task_id !== undefined) body.task_id = input.task_id;
        if (input.note !== undefined) body.note = input.note;
        // D-3-06: NO end, NO hours — their absence is the "ongoing" signal.

        const result = await previewOrCall<{ time_entry: unknown; meta?: unknown }>(
          client,
          { requireConfirm: config.KEEPING_REQUIRE_CONFIRM, confirm: input.confirm === true },
          { method: "POST", path: `/${orgId}/time-entries`, body },
        );

        // Dry-run path: pass the would_post envelope through verbatim.
        if (result !== null && typeof result === "object" && "would_post" in result) {
          return { content: [{ type: "text", text: JSON.stringify(result, null, 2) }] };
        }

        // Confirm path: extract timer_id from the wrapper using the D-2.5-05a guard.
        const entry = extractTimeEntry(result);
        const timer_id = entry?.id ?? null;
        return { content: [{ type: "text", text: JSON.stringify({ timer_id }, null, 2) }] };
      } catch (err) {
        if (classifyAmbiguous(err)) {
          const msg = err instanceof Error ? err.message : String(err);
          return { isError: true, content: [{ type: "text", text: `${AMBIGUOUS_TEXT} (${msg})` }] };
        }
        return toIsErrorContent(err);
      }
    }
    ```

    No `organisations()` call needed — start-timer per D-3-06 always uses `times`-mode shape with `start` body (no end / no hours). If the org is in `hours` mode, the API will respond appropriately; the tool surfaces that via toIsErrorContent. Documenting that decision in the header comment is fine.

    Run tests + biome + tsc.

    Commit on GREEN: `feat(03-05): keeping_start_timer — POST with no end/no hours, timer_id extraction via three-clause guard (TIMER-01, D-3-06)`.
  </action>

  <verify>
    <automated>npx vitest run test/tools/start-timer.test.ts 2>&1 | tail -10 && npx biome check src/tools/start-timer.ts 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File `src/tools/start-timer.ts` exists with `export function registerStartTimer`.
    - File contains the literal `"keeping_start_timer"`, `"timer_id"`, `Array.isArray(candidate)` (the three-clause guard).
    - The body construction does NOT have any line that sets `body.end` or `body.hours` (verify: `grep -E '\bbody\.(end|hours)\b' src/tools/start-timer.ts` returns nothing in source code, comments documenting the omission are fine).
    - Annotations: four D-3-11 booleans match.
    - D-3-12 verbatim confirm description present.
    - No `.toISOString(` outside comments.
    - `npx vitest run test/tools/start-timer.test.ts` exits 0; all 9 tests pass.
    - Full suite `npx vitest run` exits 0.
    - biome + tsc clean.
    - `src/server.ts` unmodified.
    - `git log -1 --format=%s` starts with `feat(03-05):`.
  </acceptance_criteria>

  <done>GREEN: start-timer vertical slice complete; strict body omits end/hours; timer_id extraction via verbatim three-clause guard; DST default time + date.</done>
</task>

</tasks>

<verification>
```
npx vitest run 2>&1 | tail -10
npx biome check src/ test/ 2>&1 | tail -5
npx tsc --noEmit 2>&1 | tail -5
```
</verification>

<success_criteria>
- [ ] `src/tools/start-timer.ts` registers `keeping_start_timer`
- [ ] Body always omits `end` and `hours` (strict)
- [ ] Defaults: date → `todayInAmsterdam()`, start → `nowInAmsterdamHHMM()`
- [ ] Returns `{ timer_id }` via the D-2.5-05a three-clause guard
- [ ] Drift returns `{ timer_id: null }` instead of crashing
- [ ] 9 tests pass; full suite clean
- [ ] Two commits: `test(03-05):` then `feat(03-05):`
</success_criteria>

<output>
Create `.planning/phases/03-write-tools-conditional-timers/03-05-SUMMARY.md` confirming Object.keys strictness, the three-clause guard line count match (copied verbatim from timer-status.ts), and DST default test passed.
</output>
