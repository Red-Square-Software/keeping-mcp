---
phase: 03-write-tools-conditional-timers
plan: 07
type: tdd
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/tools/resume-timer.ts
  - test/tools/resume-timer.test.ts
autonomous: true
requirements:
  - TIMER-01
  - TIMER-02
  - WRITE-04
  - WRITE-05
  - WRITE-07
tags:
  - keeping-mcp
  - phase-3
  - write-tools
  - timer-tools
  - mcp-server
  - resume-timer

must_haves:
  truths:
    - "MCP client calling keeping_resume_timer with confirm: true POSTs /{orgId}/time-entries/{entry_id}/resume exactly once via client.requestWithHeaders<T>('POST', path) (D-3-05 — resume is POST, NOT PATCH)"
    - "Tool surfaces server_time_ms from X-Server-Time-Ms with the same Number.isFinite + fallback + warn pattern as stop-timer (TIMER-02, D-3-19)"
    - "Tool surfaces the API response wrapper VERBATIM — does NOT assert input.entry_id === response.time_entry.id (per Pitfall 6: 200 vs 201, resume on new day creates new entry with different id)"
    - "Tool description SHOULD document the 200-vs-201 asymmetry (planner discretion on exact wording)"
    - "Dry-run preview: `{ would_post: { method: 'POST', url, body: null } }`"
    - "5xx → ambiguous envelope; 4xx (including 403 on locked entries per OpenAPI) → toIsErrorContent"
    - "Annotations: D-3-11 four booleans"
  artifacts:
    - path: "src/tools/resume-timer.ts"
      provides: "registerResumeTimer(server, client, config) for keeping_resume_timer"
      contains: "export function registerResumeTimer"
      contains_also: "/resume"
      contains_third: "X-Server-Time-Ms"
    - path: "test/tools/resume-timer.test.ts"
      provides: "Per-tool test set + 200-vs-201 id-asymmetry coverage (Pitfall 6)"
      min_lines: 200
  key_links:
    - from: "src/tools/resume-timer.ts confirm branch"
      to: "client.requestWithHeaders<T>('POST', path)"
      via: "POST per D-3-05 (resume is the original D-32-R POST claim — unchanged)"
      pattern: "requestWithHeaders<"
    - from: "src/tools/resume-timer.ts"
      to: "POST /resume endpoint"
      via: "`/${orgId}/time-entries/${entry_id}/resume`"
      pattern: "/resume"
---

<objective>
Ship `keeping_resume_timer`. POST `/{orgId}/time-entries/{entry_id}/resume` via `client.requestWithHeaders<T>("POST", path)` (D-3-05 — POST is correct here; only `stop` was the PATCH correction). Surface `server_time_ms` from `X-Server-Time-Ms` with the same fallback pattern as stop-timer.

Critical correctness: per Pitfall 6, resume can return 200 OR 201. 200 = modified existing entry (same id). 201 = created new ongoing entry (different id — new day rollover). The tool MUST NOT assert id-equality between input and response. It surfaces the response wrapper `{ time_entry, meta }` verbatim plus the server_time_ms anchor.

Purpose: TIMER-01 (resume portion) + TIMER-02.

Output: One new tool file + one new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-write-tools-conditional-timers/03-CONTEXT.md
@.planning/phases/03-write-tools-conditional-timers/03-RESEARCH.md
@.planning/phases/03-write-tools-conditional-timers/03-PATTERNS.md
@.planning/phases/03-write-tools-conditional-timers/03-01-SUMMARY.md

# Foundation
@src/keeping/write-gate.ts
@src/keeping/client.ts
@src/keeping/errors.ts
@src/logger.ts

# Sibling patterns
@src/tools/timer-status.ts
@test/tools/timer-status.test.ts

<interfaces>
- `client.requestWithHeaders<T>("POST", path): Promise<{ body: T; headers: Headers }>` (no body argument needed — resume takes no body per OpenAPI)
- `client.log.warn(...)` for fallback path
- `classifyAmbiguous`, `AMBIGUOUS_TEXT`
- Same inline-gate pattern as stop-timer
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → registerResumeTimer handler | Same Zod entry_id guard. |
| Same X-Server-Time-Ms surface as stop-timer | Same Number.isFinite validation + fallback. |
| Locked entries → 403 | Per OpenAPI: cannot resume locked entries. 403 surfaces as definite-fail via toIsErrorContent (4xx flows through). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Tampering | Verb mistake — PATCH instead of POST | mitigate | D-3-05: resume is POST (D-32-R's POST claim correct here; only `stop` was corrected). Test 2 strict-asserts the call used `"POST"`. |
| T-03-07-02 | Tampering | id-assumption (assuming response.time_entry.id === input.entry_id) | mitigate | Tool does NOT assert id equality. Test 5 covers the 201-with-different-id case explicitly (Pitfall 6). |
| T-03-07-03 | Tampering | X-Server-Time-Ms header parsing | mitigate | Same Number.isFinite gate as stop-timer (T-03-06-01/02). |
| T-03-07-04 | Information Disclosure | Same log.warn fixed-string + token redaction as stop-timer | mitigate | Identical to T-03-06-04 mitigation. |
| T-03-07-SC | Tampering | npm installs | accept | No installs. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — test/tools/resume-timer.test.ts including 200-vs-201 id-asymmetry</name>
  <files>test/tools/resume-timer.test.ts</files>

  <read_first>
    - test/tools/stop-timer.test.ts (if Plan 03-06 landed first; if not, fall back to test/tools/timer-status.test.ts as the canonical skeleton)
    - 03-CONTEXT.md §D-3-05 (resume = POST), D-3-09 resume_timer input, D-3-22, D-3-25 pattern
    - 03-PATTERNS.md §src/tools/resume-timer.ts (200-vs-201 awareness)
    - 03-RESEARCH.md §Pitfall 6 (200-vs-201; "make sure to check time_entry.id" warning)
  </read_first>

  <behavior>
    test/tools/resume-timer.test.ts (~9 tests). Same buildClient + constants pattern as stop-timer.test.ts. Mock surface includes `requestWithHeaders`.

    1. **Test 1: Dry-run preview — no API call.**
       Call with `{ entry_id: 12345 }`. Assert parsed response has `would_post.method === "POST"` (NOT PATCH), `would_post.url === "https://api.keeping.nl/v1/47666/time-entries/12345/resume"`, `would_post.body === null`. `calls.length === 0`.

    2. **Test 2: Confirm path POSTs via requestWithHeaders.**
       Mock returns `{ body: { time_entry: { id: 12345, ongoing: true, ... }, meta: {} }, headers: makeHeaders({ "X-Server-Time-Ms": "1718202000000" }) }`. Call with `confirm: true`. Assert `calls.length === 1`, `calls[0].method === "POST"`, `calls[0].path === "/47666/time-entries/12345/resume"`. Response contains `time_entry.id === 12345` AND `server_time_ms === 1718202000000`.

    3. **Test 3: Env-false escape** — `KEEPING_REQUIRE_CONFIRM: false`, no confirm → requestWithHeaders called.

    4. **Test 4: Missing X-Server-Time-Ms → fallback + warn.** Same as stop-timer Test 4.

    5. **Test 5: 201 response — different id → tool surfaces it verbatim (Pitfall 6).**
       Mock returns `{ body: { time_entry: { id: 99999, ongoing: true, date: "2026-06-13", ... }, meta: {} }, headers: makeHeaders({ "X-Server-Time-Ms": "1718202000000" }) }` (different id than input.entry_id of 12345). Call with `{ entry_id: 12345, confirm: true }`. Assert response `time_entry.id === 99999` (NOT 12345). Tool MUST NOT throw or error on the id mismatch.

    6. **Test 6: MultiOrgError verbatim.**

    7. **Test 7: 403 on locked entry → definite-fail via toIsErrorContent (per OpenAPI: cannot resume locked entries).**
       Mock `requestWithHeaders` throws `new KeepingApiError(403, "cannot resume locked entry")`. Assert `res.isError === true` AND text contains `"Keeping API error 403"` AND text does NOT contain `"outcome unknown"`.

    8. **Test 8: 5xx → ambiguous envelope.**

    9. **Test 9: Annotation assertion** — D-3-11 four booleans for `keeping_resume_timer`.

    All RED.
  </behavior>

  <action>
    Create `test/tools/resume-timer.test.ts`. Reuse `makeHeaders` helper and buildClient pattern from stop-timer.test.ts (if landed) or inline-define.

    Implement 9 tests above. Test 5 is the critical Pitfall 6 coverage.

    Run vitest; expect RED.

    Commit: `test(03-07): add keeping_resume_timer tests — POST verb, X-Server-Time-Ms, 200-vs-201 id asymmetry, 403 definite-fail (TIMER-01, TIMER-02, D-3-05, Pitfall 6)`.
  </action>

  <verify>
    <automated>npx vitest run test/tools/resume-timer.test.ts 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - File `test/tools/resume-timer.test.ts` exists with at least 9 `it(` declarations.
    - Contains literal `"keeping_resume_timer"`, `"/resume"`, `"X-Server-Time-Ms"`, `"server_time_ms"`, and explicit Pitfall 6 coverage (a test where input.entry_id !== response.time_entry.id).
    - vitest exits non-zero (RED).
    - No `src/tools/resume-timer.ts` exists yet.
    - `git log -1 --format=%s` starts with `test(03-07):`.
  </acceptance_criteria>

  <done>RED phase: resume-timer contract pinned including the POST verb (NOT PATCH) and the 200-vs-201 id asymmetry guarantee.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement src/tools/resume-timer.ts</name>
  <files>src/tools/resume-timer.ts</files>

  <read_first>
    - 03-PATTERNS.md §src/tools/resume-timer.ts
    - 03-CONTEXT.md §D-3-05 (resume = POST), D-3-09, D-3-11, D-3-12, D-3-18, D-3-19
    - 03-RESEARCH.md §Pitfall 6
    - src/tools/stop-timer.ts (closest sibling — same structure, change `PATCH` → `POST` and `/stop` → `/resume`, adjust description)
    - The failing tests
  </read_first>

  <action>
    Create `src/tools/resume-timer.ts`. Copy `src/tools/stop-timer.ts` as the structural starting point. Modifications:

    1. Change tool name to `"keeping_resume_timer"`, title `"Resume a stopped timer"`.
    2. Change HTTP verb from `"PATCH"` to `"POST"` in BOTH the dry-run `would_post.method` field AND the `requestWithHeaders` call.
    3. Change endpoint path from `/stop` to `/resume`.
    4. Update description to document the 200-vs-201 asymmetry (planner draft): `"Resume a previously-stopped time entry as an ongoing timer. Implemented as POST /{orgId}/time-entries/{entry_id}/resume. Returns the resumed entry plus server_time_ms (TIMER-02). NOTE: Keeping may return a different time_entry.id than the input entry_id — when the original entry's date is no longer 'today', Keeping creates a NEW ongoing entry rather than modifying the old one. Always read time_entry.id from the response, do not assume it matches your input. DRY-RUN BY DEFAULT — call without confirm first to receive a would_post preview. Cannot resume locked entries (403)."`
    5. Update warn message to `"X-Server-Time-Ms header missing on resume response; falling back to local clock"`.
    6. Header comment cites `TIMER-01, TIMER-02, D-3-05, D-3-09, D-3-11, D-3-12, D-3-16, D-3-18, D-3-19, Pitfall 6` and notes the verb is POST (D-32-R unchanged for resume) AND that the tool deliberately does NOT validate `response.time_entry.id === input.entry_id` (Pitfall 6).

    Everything else — Zod schema (entry_id required + organisation_id + confirm), annotations (D-3-11), inline-gate pattern, fallback path, catch arm — is IDENTICAL to stop-timer.

    Run tests + biome + tsc.

    Commit on GREEN: `feat(03-07): keeping_resume_timer — POST /resume via requestWithHeaders, 200-vs-201 id asymmetry surfaced verbatim (TIMER-01, TIMER-02, D-3-05, Pitfall 6)`.
  </action>

  <verify>
    <automated>npx vitest run test/tools/resume-timer.test.ts 2>&1 | tail -10 && npx biome check src/tools/resume-timer.ts 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File `src/tools/resume-timer.ts` exists with `export function registerResumeTimer`.
    - File contains literal `"keeping_resume_timer"`, `"POST"`, `"/resume"`, `"X-Server-Time-Ms"`, `requestWithHeaders<`, `Number.isFinite(parsed)`.
    - File contains the verbatim warn message `"X-Server-Time-Ms header missing on resume response; falling back to local clock"`.
    - File does NOT contain `"PATCH"` literal (this tool uses POST): `grep -c '"PATCH"' src/tools/resume-timer.ts` returns 0.
    - File does NOT contain any check that compares `input.entry_id` to a response field (Pitfall 6 — explicitly NOT validated): `grep -c 'input\.entry_id ===' src/tools/resume-timer.ts` returns 0 (allow comments mentioning the policy).
    - Annotations: four D-3-11 booleans match.
    - D-3-12 verbatim confirm description present.
    - `npx vitest run test/tools/resume-timer.test.ts` exits 0; all 9 tests pass.
    - Full suite `npx vitest run` exits 0.
    - biome + tsc clean.
    - `src/server.ts` unmodified.
    - `git log -1 --format=%s` starts with `feat(03-07):`.
  </acceptance_criteria>

  <done>GREEN: resume-timer vertical slice complete; POST verb correct; X-Server-Time-Ms surfaced; 200-vs-201 id asymmetry handled by NOT asserting id equality; 403 on locked entries surfaces as definite-fail.</done>
</task>

</tasks>

<verification>
```
npx vitest run 2>&1 | tail -10
npx biome check src/ test/ 2>&1 | tail -5
npx tsc --noEmit 2>&1 | tail -5
```
</verification>

<success_criteria>
- [ ] `src/tools/resume-timer.ts` registers `keeping_resume_timer`
- [ ] Verb is POST (NOT PATCH) per D-3-05 (only stop was the PATCH correction)
- [ ] Uses `requestWithHeaders<T>("POST", path)`
- [ ] Surfaces `server_time_ms` (TIMER-02)
- [ ] Does NOT assert id equality between input and response (Pitfall 6)
- [ ] 403 on locked entries surfaces as definite-fail via toIsErrorContent
- [ ] 9 tests pass; full suite clean
- [ ] Two commits: `test(03-07):` then `feat(03-07):`
</success_criteria>

<output>
Create `.planning/phases/03-write-tools-conditional-timers/03-07-SUMMARY.md` confirming Pitfall 6 coverage (Test 5 with different ids) and the description-copy includes the id-asymmetry note.
</output>
