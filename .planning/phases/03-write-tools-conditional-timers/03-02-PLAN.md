---
phase: 03-write-tools-conditional-timers
plan: 02
type: tdd
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/tools/add-entry.ts
  - test/tools/add-entry.test.ts
autonomous: true
requirements:
  - WRITE-01
  - WRITE-04
  - WRITE-05
  - WRITE-06
  - WRITE-07
  - WRITE-08
tags:
  - keeping-mcp
  - phase-3
  - write-tools
  - mcp-server
  - add-entry

must_haves:
  truths:
    - "MCP client calling keeping_add_entry without confirm (KEEPING_REQUIRE_CONFIRM=true) receives `{ would_post: { method: 'POST', url: 'https://api.keeping.nl/v1/{orgId}/time-entries', body } }` and the mock KeepingClient.post was NOT called (D-3-01, D-3-02, WRITE-04)"
    - "MCP client calling keeping_add_entry with confirm: true POSTs to /{orgId}/time-entries exactly once with the constructed body (WRITE-01, D-3-05)"
    - "When KEEPING_REQUIRE_CONFIRM=false, keeping_add_entry POSTs without confirm (D-3-01 env-false escape hatch)"
    - "When org features.timesheet === 'times', body contains start (defaults to nowInAmsterdamHHMM()) and may contain end; no hours key (D-3-08)"
    - "When org features.timesheet === 'hours', body contains hours; no start/end keys; missing hours → isError with helpful message (D-3-08)"
    - "When date is omitted, body.date equals todayInAmsterdam() — DST-correct against Date.now() = 2026-06-12T22:30:00Z → '2026-06-13' (WRITE-08, D-3-15, D-3-26)"
    - "purpose Zod enum exactly matches D-3-07: ['work','break','special_leave','unpaid_leave','statutory_leave','sick_leave','work_reduction','trip']; default 'work'"
    - "Tool annotations: readOnlyHint:false, destructiveHint:true, idempotentHint:false, openWorldHint:true (D-3-11, WRITE-07)"
    - "5xx server error from client.post surfaces as isError envelope with text starting `outcome unknown — verify with keeping_list_entries before retrying.` (D-3-16, WRITE-05)"
    - "401 KeepingAuthError surfaces verbatim D-25 message; MultiOrgError surfaces verbatim D-27 template; 4xx KeepingApiError surfaces with original message (D-3-20, SAFE-04)"
    - "Date.toISOString() does NOT appear anywhere in src/tools/add-entry.ts (D-3-13)"
  artifacts:
    - path: "src/tools/add-entry.ts"
      provides: "registerAddEntry(server, client, config) MCP tool registration for keeping_add_entry"
      contains: "export function registerAddEntry"
      contains_also: "previewOrCall"
      contains_third: "todayInAmsterdam"
    - path: "test/tools/add-entry.test.ts"
      provides: "Full per-tool test set per D-3-22 + DST default-date test (D-3-26)"
      min_lines: 250
  key_links:
    - from: "src/tools/add-entry.ts"
      to: "src/keeping/write-gate.ts"
      via: "import { previewOrCall, classifyAmbiguous, AMBIGUOUS_TEXT }"
      pattern: "from \"\\.\\./keeping/write-gate\\.js\""
    - from: "src/tools/add-entry.ts"
      to: "src/keeping/date.ts"
      via: "import { todayInAmsterdam, nowInAmsterdamHHMM }"
      pattern: "from \"\\.\\./keeping/date\\.js\""
    - from: "src/tools/add-entry.ts"
      to: "KeepingClient.organisations()"
      via: "await client.organisations() then find org by id to read features.timesheet"
      pattern: "client\\.organisations\\("
---

<objective>
Ship `keeping_add_entry` as a complete vertical slice (Zod input schema → handler → 11 tests). Tool calls `POST /{orgId}/time-entries` via the Phase 03-01 `previewOrCall` write-gate, defaults `date` to `todayInAmsterdam()`, branches on `org.features.timesheet` ("times" → start/end HH:mm; "hours" → hours decimal), surfaces ambiguous failures via `classifyAmbiguous` + `AMBIGUOUS_TEXT`. The `confirm` parameter carries the D-3-12 verbatim description. Server-registration is deferred to Plan 03-08 to keep this slice file-conflict-free.

Purpose: WRITE-01, WRITE-04, WRITE-05, WRITE-06, WRITE-07, WRITE-08 all addressed in one tool. After this plan, a MCP-connected client could (modulo server.ts wiring in 03-08) preview-then-confirm a new time entry end-to-end.

Output: One new file in `src/tools/`, one new sibling test file in `test/tools/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-write-tools-conditional-timers/03-CONTEXT.md
@.planning/phases/03-write-tools-conditional-timers/03-RESEARCH.md
@.planning/phases/03-write-tools-conditional-timers/03-PATTERNS.md
@.planning/phases/03-write-tools-conditional-timers/03-01-SUMMARY.md

# Foundation this plan consumes (from 03-01)
@src/keeping/write-gate.ts
@src/keeping/date.ts
@src/keeping/client.ts
@src/keeping/types.ts
@src/keeping/errors.ts

# Sibling pattern — verbatim skeleton to mirror
@src/tools/timer-status.ts
@test/tools/timer-status.test.ts

# Sibling pattern — Zod 4 input schema reference
@src/tools/entries-list.ts

# Anonymised real entry fixture for response mocks
@test/fixtures/time-entry-response.sample.json

<interfaces>
<!-- Consumed from 03-01 foundation -->

`src/keeping/write-gate.ts`:
- `previewOrCall<T>(client: KeepingClient, cfg: { requireConfirm: boolean; confirm: boolean }, req: { method: WriteMethod; path: string; body?: unknown }): Promise<WouldPost | T>`
- `classifyAmbiguous(err: unknown): boolean`
- `AMBIGUOUS_TEXT: "outcome unknown — verify with keeping_list_entries before retrying."`

`src/keeping/date.ts`:
- `todayInAmsterdam(now?: Date): string` — YYYY-MM-DD
- `nowInAmsterdamHHMM(now?: Date): string` — HH:mm

`src/keeping/client.ts`:
- `client.resolveOrgId(input?: string): Promise<string>`
- `client.organisations(): Promise<KeepingOrg[]>` (cached)
- `client.post<T>(path, body): Promise<T>`
- `KeepingOrg.features.timesheet: "times" | "hours"` (already typed)

`src/config.ts`:
- `KeepingConfig.KEEPING_REQUIRE_CONFIRM: boolean`

`src/keeping/types.ts`:
- `EntryCreateBody`, `TimeEntryResponse`
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → registerAddEntry handler | Zod parses input args; rejects path-traversal vectors by typing `project_id`/`task_id` as `z.number().int()` only. |
| Tool handler → KeepingClient.post | All path segments built via template literal from `${orgId}` (numeric-string from `resolveOrgId`). No user-controlled segments. |
| Preview body echo (would_post.body) → MCP client | `note` may contain confidential text. By design (preview = user reviews); documented in README per Phase 4. NOT a leak path here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | `confirm` Zod field default | mitigate | `confirm: z.boolean().optional()` — NO `.default(true)`. Test 1 verifies dry-run path is taken when `confirm` is omitted. A `.default(true)` typo would silently bypass the gate. |
| T-03-02-02 | Tampering | AND-gate logic in tool handler | mitigate | Tool relies on `previewOrCall(client, { requireConfirm: config.KEEPING_REQUIRE_CONFIRM, confirm: input.confirm === true }, ...)` — strict triple-equals coercion of `confirm`. Inputs like `"true"`, `1`, `{}` collapse to `false`. Test 8 covers `confirm: "true"` (string) → preview path. |
| T-03-02-03 | Elevation of Privilege | `user_id` field absence (D-3-10) | mitigate | Zod schema does NOT include `user_id`. Even if a client supplies it, Zod strips it. Test 9 confirms posted body has no `user_id` key. Keeping defaults `user_id` to authenticated user. |
| T-03-02-04 | Information Disclosure | KeepingApiError surfacing token | mitigate | `KeepingApiError.message` already runs through `sanitiseBody` in `rawFetch` (Phase 2 carry-forward). Tool handler simply forwards via `toIsErrorContent`. No new sanitisation needed. Test 7 (4xx KeepingApiError surface) asserts message verbatim. |
| T-03-02-05 | Tampering | `purpose` Zod enum | mitigate | `z.enum([...8 values...]).default("work")` — Zod rejects any string outside the 8-value enum BEFORE the body reaches the API. Test 10 asserts a `purpose: "invalid"` input is rejected with `isError: true`. |
| T-03-02-06 | Information Disclosure | Date.toISOString() leaking UTC day | mitigate | `todayInAmsterdam()` from 03-01 is the only date source; `Date.toISOString()` forbidden. Grep gate: `grep -v '^//' src/tools/add-entry.ts \| grep -c '\.toISOString('` returns 0. |
| T-03-02-SC | Tampering | npm installs | accept | No package installs. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — test/tools/add-entry.test.ts with full D-3-22 per-tool test set + DST default-date</name>
  <files>test/tools/add-entry.test.ts</files>

  <read_first>
    - test/tools/timer-status.test.ts (full — canonical skeleton: `buildClient(mockClient)` + `InMemoryTransport.createLinkedPair()` + happy + error + annotation tests)
    - test/fixtures/time-entry-response.sample.json (structural reference for mock POST response)
    - src/keeping/errors.ts (KeepingApiError, KeepingAuthError, MultiOrgError shapes for error-path tests)
    - src/config.ts (KeepingConfig shape — `{ KEEPING_TOKEN, KEEPING_REQUIRE_CONFIRM, KEEPING_ORG_ID?, KEEPING_LOG_LEVEL }`)
    - 03-CONTEXT.md §D-3-01, D-3-02, D-3-04, D-3-07, D-3-08, D-3-09 (`keeping_add_entry` input fields), D-3-11, D-3-12, D-3-15, D-3-16, D-3-20, D-3-22, D-3-26, D-3-28
    - 03-PATTERNS.md §src/tools/add-entry.ts + §test/tools/<tool>.test.ts files (test harness pattern)
    - 03-RESEARCH.md §"Verified entry_create_request shape" (times mode + hours mode body examples) + §Pattern 1 sketch + §Pitfall 7 (meta wrapper passthrough)
  </read_first>

  <behavior>
    test/tools/add-entry.test.ts (~11 tests). Structure mirrors test/tools/timer-status.test.ts. Build a `buildClient(mockClient, config?)` helper that registers `registerAddEntry(server, mockClient, config)` and returns a linked MCP Client.

    Shared `mockOrgTimes` constant (for times-mode tests):
    `{ id: 47666, name: "Acme", url: "https://acme.keeping.nl", current_plan: "plus_2019", features: { timesheet: "times", projects: true, tasks: true, breaks: false }, time_zone: "Europe/Amsterdam", currency: "EUR" }`.

    Shared `mockOrgHours` constant: same shape with `features.timesheet: "hours"`.

    Shared `defaultConfig: KeepingConfig`: `{ KEEPING_TOKEN: "kp_test_FAKE", KEEPING_REQUIRE_CONFIRM: true, KEEPING_LOG_LEVEL: "error" }`.

    Tests:

    1. **Test 1: Dry-run (env=true, confirm omitted) returns preview, post NOT called.**
       Mock: `resolveOrgId → "47666"`, `organisations → [mockOrgTimes]`, `post → throw new Error("post should not be called")`. Call tool with `{ date: "2026-06-12", purpose: "work", start: "13:45", end: "15:15" }`. Assert `res.isError` falsy, parsed JSON has top-level `would_post.method === "POST"`, `would_post.url === "https://api.keeping.nl/v1/47666/time-entries"`, `would_post.body.date === "2026-06-12"`, `would_post.body.purpose === "work"`, `would_post.body.start === "13:45"`, `would_post.body.end === "15:15"`, `would_post.body.hours` undefined.

    2. **Test 2: Confirm path (env=true, confirm:true) calls post exactly once with expected path+body.**
       Track calls in a `posts: { path, body }[]` array. Mock `post` returns `{ time_entry: { id: 999, date: "2026-06-12", ongoing: false }, meta: {} }`. Call with same args + `confirm: true`. Assert `posts.length === 1`, `posts[0].path === "/47666/time-entries"`, `posts[0].body.date === "2026-06-12"`, `posts[0].body.purpose === "work"`, `posts[0].body.start === "13:45"`, `posts[0].body.end === "15:15"`. Response: `res.isError` falsy, parsed JSON deep-equals the mock response wrapper.

    3. **Test 3: Env-false escape hatch (`KEEPING_REQUIRE_CONFIRM: false`, no confirm) → post called.**
       Use config with `KEEPING_REQUIRE_CONFIRM: false`. Call WITHOUT `confirm`. Assert post was called exactly once with the constructed body.

    4. **Test 4: MultiOrgError flows through toIsErrorContent verbatim (D-27).**
       Mock `resolveOrgId` throws `new MultiOrgError([{ id: 100, name: "Acme" }, { id: 200, name: "Beta" }])`. Call tool with `{ purpose: "work", start: "13:45" }`. Assert `res.isError === true` AND text equals `"Multiple organisations available. Pass organisation_id, or set KEEPING_ORG_ID. Options: 100 (Acme), 200 (Beta)."` (byte-exact D-27).

    5. **Test 5: 401 KeepingAuthError flows through toIsErrorContent verbatim (D-25).**
       Mock `resolveOrgId → "47666"`, `organisations → [mockOrgTimes]`, `post → throw new KeepingAuthError()`. Call with `confirm: true`. Assert `res.isError === true` AND text equals `"Keeping rejected the token. Verify KEEPING_TOKEN and restart the MCP server."` (byte-exact D-25).

    6. **Test 6: 4xx KeepingApiError (definite-fail) flows through toIsErrorContent, NOT ambiguous envelope.**
       Mock `post → throw new KeepingApiError(422, '{"error":{"message":"validation"}}')`. Call with `confirm: true`. Assert `res.isError === true` AND text contains `"Keeping API error 422"` AND text does NOT contain `"outcome unknown"`.

    7. **Test 7: 5xx KeepingApiError (ambiguous) → ambiguous envelope with "outcome unknown" text + original message in parenthetical.**
       Mock `post → throw new KeepingApiError(503, "service unavailable")`. Call with `confirm: true`. Assert `res.isError === true` AND text starts with `"outcome unknown — verify with keeping_list_entries before retrying."` AND text contains `"Keeping API error 503"` (the original message) wrapped in `(...)`.

    8. **Test 8: confirm: "true" (string, not boolean) is coerced to false → preview path.**
       Test the strict `=== true` coercion. Mock as in Test 1. Call with `{ purpose: "work", start: "13:45", confirm: "true" as unknown as boolean }`. Note: Zod rejects non-boolean for `z.boolean().optional()`, so the call will return an isError with a Zod validation message — this is the correct safe behavior. Assert `res.isError === true` AND text contains either `"Expected boolean"` or `"Invalid"` (Zod message variability). Post NOT called.

    9. **Test 9: user_id input is stripped by Zod schema (D-3-10).**
       Call with `{ purpose: "work", start: "13:45", user_id: "evil", confirm: true } as unknown as Parameters<typeof client.callTool>[0]["arguments"]`. The MCP client may pass it through; the Zod schema MUST not include `user_id` so it gets stripped before reaching the post body. Assert `posts[0].body.user_id === undefined`. (If MCP-SDK's listTools schema enforcement strips unknowns before reaching the handler, this is a no-op success; if not, the assertion catches a planner mis-schema.)

    10. **Test 10: Annotation assertion via listTools (D-3-11).**
        Call `client.listTools()`; find tool with name `"keeping_add_entry"`; assert `annotations.readOnlyHint === false`, `annotations.destructiveHint === true`, `annotations.idempotentHint === false`, `annotations.openWorldHint === true`.

    11. **Test 11: DST-correct date default (D-3-15, D-3-26, D-3-28).**
        Override `Date.now()` for the duration of this test: `vi.useFakeTimers(); vi.setSystemTime(new Date("2026-06-12T22:30:00Z"));` (or simpler: use `vi.spyOn(globalThis, "Date").mockImplementation` if the helpers explicitly pass `now`; if helpers use `new Date()` internally, `vi.setSystemTime` is the lever). Use the same mock setup as Test 1 (dry-run). Call with `{ purpose: "work" }` — NO `date`, NO `start`. Assert `would_post.body.date === "2026-06-13"` (next day Amsterdam, CEST). If the handler also auto-defaults `start` when omitted in `times` mode, also assert `would_post.body.start === "00:30"`. Restore timers in `afterEach`. (NOTE: if `start` is not auto-defaulted in this slice, drop the start assertion; D-3-28 mandates the HH:mm shape — adjust per the implementation choice taken in Task 2.)

    Bonus optional (~12th): hours-mode body construction.

    12. **Test 12: Hours-mode org, missing `hours` input → isError envelope explaining hours required (D-3-08).**
        Use config + mock with `mockOrgHours`. Call with `{ purpose: "work" }` (no hours). Assert `res.isError === true` AND text contains `"hours"` AND `"hours" mode` (or similar verbatim text from D-3-08).

    13. **Test 13: Hours-mode org, hours: 1.5 → body has hours, no start/end (D-3-08).**
        Mock with `mockOrgHours`. Call with `{ purpose: "work", hours: 1.5, confirm: true }`. Assert `posts[0].body.hours === 1.5`, `posts[0].body.start === undefined`, `posts[0].body.end === undefined`, `posts[0].body.date === todayInAmsterdam()` (computed from current time in the test).

    All tests RED initially because `src/tools/add-entry.ts` does not yet exist.
  </behavior>

  <action>
    Create `test/tools/add-entry.test.ts`. Top: imports per `03-PATTERNS.md §test/tools/<tool>.test.ts` (verbatim list — Client, InMemoryTransport, McpServer, describe/expect/it/vi from vitest, KeepingClient type, KeepingApiError/KeepingAuthError/MultiOrgError, and the not-yet-existing `registerAddEntry` from `../../src/tools/add-entry.js`).

    Implement `buildClient(mockClient, config = defaultConfig)` helper. Implement shared `mockOrgTimes`, `mockOrgHours`, `defaultConfig` constants.

    Write all 11–13 tests above. Use `JSON.parse(content[0]?.text ?? "")` to inspect handler output. Use `expect(...).toBe(...)` for byte-exact D-25/D-27/AMBIGUOUS_TEXT assertions; `expect(...).toEqual(...)` for object deep equality.

    Run `npx vitest run test/tools/add-entry.test.ts 2>&1 | tail -10` — expect failures (module not found OR cannot find registerAddEntry — RED).

    Commit on RED: `test(03-02): add keeping_add_entry tests — dry-run, confirm, env-false, error paths, annotations, DST default (D-3-22, D-3-26)`.

    Do NOT modify src/.
  </action>

  <verify>
    <automated>npx vitest run test/tools/add-entry.test.ts 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - File `test/tools/add-entry.test.ts` exists.
    - File contains the literal strings `"keeping_add_entry"`, `"would_post"`, `"https://api.keeping.nl/v1/47666/time-entries"`, the byte-exact D-25 message `"Keeping rejected the token. Verify KEEPING_TOKEN and restart the MCP server."`, the byte-exact D-27 message `"Multiple organisations available. Pass organisation_id, or set KEEPING_ORG_ID. Options: 100 (Acme), 200 (Beta)."`, the byte-exact AMBIGUOUS_TEXT `"outcome unknown — verify with keeping_list_entries before retrying."`, AND the literal `"2026-06-13"` (DST assertion).
    - File contains at least 11 `it(` declarations.
    - `npx vitest run test/tools/add-entry.test.ts` exits non-zero (RED).
    - No `src/tools/add-entry.ts` exists yet.
    - `git log -1 --format=%s` outputs a message starting with `test(03-02):`.
  </acceptance_criteria>

  <done>RED phase complete: full add-entry test set written; all assertions fail because the tool file does not exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement src/tools/add-entry.ts to make all tests pass</name>
  <files>src/tools/add-entry.ts</files>

  <read_first>
    - 03-RESEARCH.md §Pattern 1 (sketch — handler structure including org-mode branching)
    - 03-PATTERNS.md §src/tools/add-entry.ts (full pattern with imports, Zod schema, handler body, write-tool variant of registerXxx)
    - 03-CONTEXT.md §D-3-01..09 (input fields + AND-gate + body shape), §D-3-11 (annotations), §D-3-12 (confirm description), §D-3-16 (ambiguous envelope), §D-3-28 (HH:mm not ISO)
    - src/tools/timer-status.ts (verbatim skeleton — copy structure, change READ paths to WRITE paths, flip annotations, add config param, add classifyAmbiguous catch branch)
    - src/tools/entries-list.ts (Zod 4 input schema reference — `.regex`, `.optional`, `.describe`, `.default`)
    - src/keeping/write-gate.ts (consumer of previewOrCall, classifyAmbiguous, AMBIGUOUS_TEXT)
    - src/keeping/date.ts (consumer of todayInAmsterdam, nowInAmsterdamHHMM)
    - The failing tests in test/tools/add-entry.test.ts define the exact contract
  </read_first>

  <action>
    Create `src/tools/add-entry.ts`. Header comment cites `WRITE-01, WRITE-04..08, D-3-01, D-3-02, D-3-04, D-3-07, D-3-08, D-3-09, D-3-11, D-3-12, D-3-16, D-3-28`.

    Imports (per 03-PATTERNS.md):
    - `import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"`
    - `import { z } from "zod"`
    - `import type { KeepingClient } from "../keeping/client.js"`
    - `import type { KeepingConfig } from "../config.js"`
    - `import { toIsErrorContent } from "../keeping/errors.js"`
    - `import { previewOrCall, classifyAmbiguous, AMBIGUOUS_TEXT } from "../keeping/write-gate.js"`
    - `import { todayInAmsterdam, nowInAmsterdamHHMM } from "../keeping/date.js"`

    Zod schema `AddEntryInput` (per D-3-09):
    - `organisation_id: z.string().optional().describe("Override KEEPING_ORG_ID; required for multi-org tokens.")` — verbatim from timer-status.ts
    - `date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional().describe("Calendar date YYYY-MM-DD; defaults to today in Europe/Amsterdam.")` (WRITE-08)
    - `purpose: z.enum(["work","break","special_leave","unpaid_leave","statutory_leave","sick_leave","work_reduction","trip"]).default("work").describe("Time-entry category. Maps to Keeping's purpose enum; billable status is determined at the project level.")` (D-3-07)
    - `project_id: z.number().int().positive().optional()`
    - `task_id: z.number().int().positive().optional()`
    - `note: z.string().max(10000).optional()`
    - `tag_ids: z.array(z.number().int().positive()).optional()`
    - `external_references: z.array(z.object({ id: z.string().regex(/^[0-9a-f]{10,40}$/), type: z.literal("generic_work_reference"), name: z.string().max(191), url: z.string().max(2048).optional() })).max(10).optional()`
    - `start: z.string().regex(/^\d{1,2}:\d{2}(:\d{2})?(am|pm)?$/i).optional().describe("HH:mm in org timezone; only used when org timesheet is 'times' mode.")` (D-3-28)
    - `end: z.string().regex(/^\d{1,2}:\d{2}(:\d{2})?(am|pm)?$/i).optional().describe("HH:mm in org timezone; only used when org timesheet is 'times' mode.")`
    - `hours: z.number().min(0).max(1000).optional().describe("Decimal hours; required when org timesheet is 'hours' mode.")`
    - `confirm: z.boolean().optional().describe("Set to true ONLY after a human has reviewed the would_post preview returned by a prior dry-run call. The MCP client (LLM) MUST NOT set this autonomously — wait for the human to type 'yes' / 'confirm'.")` (D-3-12 verbatim)

    NOTE: NO `user_id` field. NO `confirm: z.boolean().default(true)` — `confirm` defaults are implicit-undefined, treated as false by the AND-gate. Strip-unknowns is the Zod default; do not call `.strict()` because MCP clients may pass extra fields the SDK already filters.

    `registerAddEntry(server, client, config)` function:
    - Tool name: `"keeping_add_entry"`
    - Title: `"Create a time entry"`
    - Description (planner draft, per D-3-12 confirm spirit + D-3-08 mode awareness): `"Create a new time entry. DRY-RUN BY DEFAULT — call without confirm first to receive a would_post preview; call again with confirm: true ONLY after a human reviewed the preview. Body shape depends on the organisation's timesheet mode: 'times' mode requires start/end (HH:mm in org timezone); 'hours' mode requires a decimal hours value. Purpose defaults to 'work'. The date defaults to today in Europe/Amsterdam."`
    - inputSchema: `AddEntryInput`
    - annotations: `{ readOnlyHint: false, destructiveHint: true, idempotentHint: false, openWorldHint: true }` (D-3-11)
    - NO outputSchema.

    Handler body (per 03-RESEARCH.md §Pattern 1 + 03-PATTERNS.md §src/tools/add-entry.ts):
    ```
    async (input) => {
      try {
        const orgId = await client.resolveOrgId(input.organisation_id);
        const orgs = await client.organisations();
        const org = orgs.find((o) => String(o.id) === orgId);
        if (!org) {
          // Defensive: resolveOrgId returned an id not in the cached list (race or cache miss).
          throw new Error(`Organisation ${orgId} not found in cache`);
        }

        const date = input.date ?? todayInAmsterdam();
        const body: Record<string, unknown> = { date, purpose: input.purpose };
        if (input.project_id !== undefined) body.project_id = input.project_id;
        if (input.task_id !== undefined) body.task_id = input.task_id;
        if (input.note !== undefined) body.note = input.note;
        if (input.tag_ids !== undefined) body.tag_ids = input.tag_ids;
        if (input.external_references !== undefined) body.external_references = input.external_references;

        if (org.features.timesheet === "times") {
          body.start = input.start ?? nowInAmsterdamHHMM();
          if (input.end !== undefined) body.end = input.end;
        } else {
          // hours mode
          if (input.hours === undefined) {
            return toIsErrorContent(
              new Error("Organisation timesheet is in 'hours' mode; 'hours' input is required.")
            );
          }
          body.hours = input.hours;
        }

        const result = await previewOrCall<{ time_entry: unknown; meta?: unknown }>(
          client,
          { requireConfirm: config.KEEPING_REQUIRE_CONFIRM, confirm: input.confirm === true },
          { method: "POST", path: `/${orgId}/time-entries`, body },
        );
        return { content: [{ type: "text", text: JSON.stringify(result, null, 2) }] };
      } catch (err) {
        if (classifyAmbiguous(err)) {
          const msg = err instanceof Error ? err.message : String(err);
          return { isError: true, content: [{ type: "text", text: `${AMBIGUOUS_TEXT} (${msg})` }] };
        }
        return toIsErrorContent(err);
      }
    }
    ```

    Run `npx vitest run test/tools/add-entry.test.ts 2>&1 | tail -10` — expect ALL tests to pass (GREEN). If a test fails, the FIX is in the source (or test if test was over-prescriptive); do NOT weaken the test to accommodate a buggy source.

    Run `npx biome check src/tools/add-entry.ts test/tools/add-entry.test.ts 2>&1 | tail -10` — expect exit 0.

    Run `npx tsc --noEmit 2>&1 | tail -10` — expect exit 0.

    Grep gate: `grep -v '^//' src/tools/add-entry.ts | grep -c '\.toISOString('` MUST return 0.

    Commit on GREEN: `feat(03-02): keeping_add_entry — dry-run gate, org-mode-aware body, Amsterdam date default (WRITE-01, D-3-07, D-3-08)`.
  </action>

  <verify>
    <automated>npx vitest run test/tools/add-entry.test.ts 2>&1 | tail -10 && npx biome check src/tools/add-entry.ts 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File `src/tools/add-entry.ts` exists.
    - File contains the literal `"keeping_add_entry"`, the verbatim D-3-12 confirm description string `"Set to true ONLY after a human has reviewed the would_post preview returned by a prior dry-run call. The MCP client (LLM) MUST NOT set this autonomously — wait for the human to type 'yes' / 'confirm'."`, the literal `"work","break","special_leave","unpaid_leave","statutory_leave","sick_leave","work_reduction","trip"` (the D-3-07 enum joined by commas in some form), and `previewOrCall`.
    - File does NOT contain `"user_id"` as a Zod field (verify: `grep -n 'user_id' src/tools/add-entry.ts` returns nothing in the Zod schema region; comments mentioning user_id-omission are fine).
    - File does NOT contain `Date.prototype.toISOString()` calls: `grep -v '^//' src/tools/add-entry.ts | grep -c '\.toISOString('` returns 0.
    - File contains all four annotation booleans matching D-3-11: `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.
    - `npx vitest run test/tools/add-entry.test.ts` exits 0 with all 11+ tests passing.
    - `npx vitest run` (full suite) exits 0 — no existing test regresses.
    - `npx biome check src/tools/add-entry.ts test/tools/add-entry.test.ts` exits 0.
    - `npx tsc --noEmit` exits 0.
    - `git log -1 --format=%s` outputs a message starting with `feat(03-02):`.
    - `src/server.ts` is UNMODIFIED (`git diff HEAD~2 -- src/server.ts 2>&1` returns empty) — registration is deferred to Plan 03-08.
  </acceptance_criteria>

  <done>GREEN phase complete: add-entry tool implements WRITE-01, dry-run gate (WRITE-04), ambiguous envelope (WRITE-05), purpose enum (WRITE-06 amended per D-3-07), annotations (WRITE-07), Amsterdam date default (WRITE-08). All 11+ tests pass.</done>
</task>

</tasks>

<verification>
After both tasks:
```
npx vitest run 2>&1 | tail -10           # all tests pass
npx biome check src/ test/ 2>&1 | tail -5 # exit 0
npx tsc --noEmit 2>&1 | tail -5           # exit 0
```

server.ts is NOT modified — Plan 03-08 wires all six write tools at the end of Phase 3.
</verification>

<success_criteria>
- [ ] `src/tools/add-entry.ts` registers `keeping_add_entry` with the full Zod input surface from D-3-09 minus `user_id`
- [ ] Dry-run gate works in both `requireConfirm:true` (preview) and `requireConfirm:false` (escape hatch) modes
- [ ] Org-mode-aware body construction: times → start/end; hours → hours
- [ ] Date defaults to `todayInAmsterdam()`; DST rollover verified for `2026-06-12T22:30:00Z` → `"2026-06-13"`
- [ ] 5xx + AbortError + TypeError surface via AMBIGUOUS_TEXT envelope; everything else via `toIsErrorContent`
- [ ] Annotations: D-3-11 four booleans
- [ ] No `Date.toISOString()` anywhere
- [ ] 11+ tests pass; full suite clean
- [ ] Two commits: `test(03-02):` then `feat(03-02):`
</success_criteria>

<output>
Create `.planning/phases/03-write-tools-conditional-timers/03-02-SUMMARY.md` documenting: test count, any test-naming refinements, any edge case discovered (e.g., MCP-SDK passes/strips extra fields differently than expected), and confirmation of byte-exact D-25/D-27/AMBIGUOUS_TEXT assertions.
</output>
