---
phase: 04-distribution-release-pipeline
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - package.json
autonomous: false
requirements: [DIST-05, REL-02, REL-05]
requirements_addressed: [DIST-05, REL-02, REL-05]
tags: [release, oidc, mcp-registry, post-publish-verification]

must_haves:
  truths:
    - "ROADMAP SC #2 happens end-to-end: pushing `v1.0.0` triggers `.github/workflows/release.yml`, npm publishes with OIDC (no token secret), provenance attestation appears on the npm package page, mcp-publisher publishes to MCP Registry under `io.github.red-square-software/keeping-mcp`, and `server.json` version is derived from package.json (not hand-edited)."
    - "ROADMAP SC #3 discovery: `https://registry.modelcontextprotocol.io/v0/servers?search=keeping-mcp` returns the published entry, and a user can install it via the registry."
    - "ROADMAP SC #5 cold-start safety: a fresh Windows 11 shell running `npx -y keeping-mcp` (without `KEEPING_TOKEN`) exits non-zero with the byte-locked stderr message `[keeping-mcp] Configuration error: KEEPING_TOKEN must not be empty` — NO `ENOENT`, NO silent hang."
    - "DIST-05 namespace claimed: post-publish, the MCP Registry shows the entry at `io.github.red-square-software/keeping-mcp` with the published version."
    - "REL-02 verified end-to-end: provenance badge visible on npmjs.com/package/keeping-mcp; `npm view keeping-mcp --json | jq '.dist.attestations'` returns a non-null attestation object."
    - "Pre-flight human action completed: npm 'pending publisher' configured on npmjs.com for `keeping-mcp` bound to `red-square-software/keeping-mcp` repo + `.github/workflows/release.yml` workflow + `main` branch (RESEARCH §Pitfall 7, §Environment Availability) — without this, the first publish hard-fails with HTTP 403."
  artifacts:
    - path: "package.json"
      provides: "Version bumped to 1.0.0 (from 0.1.0) via `npm version 1.0.0 -m 'release: %s'`"
      contains: "\"version\": \"1.0.0\""
  key_links:
    - from: "git push origin v1.0.0 (and main with bumped version)"
      to: ".github/workflows/release.yml on: push: tags: ['v*']"
      via: "GitHub Actions tag-push event"
      pattern: "v1\\.0\\.0"
    - from: "Published npm package keeping-mcp@1.0.0"
      to: "MCP Registry entry io.github.red-square-software/keeping-mcp@1.0.0"
      via: "mcp-publisher publish reading mutated server.json"
      pattern: "io\\.github\\.red-square-software\\/keeping-mcp"
    - from: "User on Windows 11 running npx -y keeping-mcp"
      to: "Phase 1 D-05 cold-start error message"
      via: "bin/keeping-mcp.ts loadConfig path"
      pattern: "KEEPING_TOKEN must not be empty"
---

<objective>
Ship `keeping-mcp@1.0.0` — the first real public release. Pre-configure the npm trusted publisher (RESEARCH §Pitfall 7, the only legitimate human-only step in Phase 4), bump `package.json` from `0.1.0` to `1.0.0`, push the tag, watch the workflow created in Plan 04-03 run end-to-end, verify all four post-publish checks (npm provenance badge, MCP Registry discoverability, `npm audit signatures`, Windows cold-start smoke), and document the published artifact in SUMMARY.

Purpose: Plans 04-01 / 04-02 / 04-03 produced the file shape but no external state. This plan creates the irreversible external state: claimed npm name `keeping-mcp@1.0.0`, claimed MCP Registry namespace `io.github.red-square-software/keeping-mcp`, provenance attestation linked to the GitHub commit. Once `v1.0.0` is published, the package name and the registry namespace are claimed forever.

Output: A live published package on npmjs.com + a live MCP Registry entry + a documented SUMMARY of post-publish verification.

**This plan is NOT autonomous.** The pre-flight pending-publisher step is a human-only action on npmjs.com; the tag push that triggers the irreversible publish is gated on explicit human approval. Execute-phase MUST pause for confirmation at every checkpoint task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-distribution-release-pipeline/04-RESEARCH.md
@.planning/phases/04-distribution-release-pipeline/04-01-SUMMARY.md
@.planning/phases/04-distribution-release-pipeline/04-02-SUMMARY.md
@.planning/phases/04-distribution-release-pipeline/04-03-SUMMARY.md
@CLAUDE.md
@package.json
@server.json
@README.md
@.github/workflows/release.yml

<interfaces>
<!-- The end-to-end artifacts in place from prior plans + the release.yml shape this plan exercises. -->

From Plan 04-01:
- `server.json` exists with `version: "0.0.0"` and `packages[0].version: "0.0.0"` placeholders.
- `npm run check-publish-shape` runs locally and on CI.

From Plan 04-02:
- `README.md` ships with Windows + macOS/Linux config blocks, token setup, dry-run warning callouts.

From Plan 04-03:
- `.github/workflows/release.yml` exists with `ci-gate` + `publish` jobs.
- Workflow triggers on `push: tags: ['v*']`.
- Publish job runs `npm run check-publish-shape` + `npm publish --provenance --access public` + jq inject + `mcp-publisher login github-oidc` + `mcp-publisher publish`.
- Job verifies `${GITHUB_REF#refs/tags/v}` matches `package.json.version` — so we MUST `npm version` BEFORE tagging.

Pre-existing post-publish surfaces (verified in RESEARCH §Environment Availability):
- `https://www.npmjs.com/package/keeping-mcp` — will exist after first publish; shows provenance badge if attestation succeeded.
- `https://registry.modelcontextprotocol.io/v0/servers?search=keeping-mcp` — currently returns 0 results; will return 1 after `mcp-publisher publish` succeeds.
- `npm view keeping-mcp --json | jq '.dist.attestations'` — returns null pre-publish; non-null after.
- `npm audit signatures` — verifies provenance signature chain.
</interfaces>

<research_excerpts>
From 04-RESEARCH.md §Pitfall 7 (THE blocker for first publish):
> First `git tag v0.1.0 && git push` triggers the workflow; OIDC mint succeeds; `npm publish` fails with "no trusted publisher configured for keeping-mcp@0.1.0". npm trusted publishing requires the publisher to be configured on the npm package settings page (npmjs.com → package → Settings → Trusted Publishers) BEFORE the first publish. For a brand-new package name, this is a chicken-and-egg unless you pre-create the package or use the `pending` flow.
> Recommendation: pre-create the package via the pending-publisher flow so the first CI publish works.

From 04-RESEARCH.md §Environment Availability:
> npm "pending publisher" pre-config — must be done manually on npmjs.com. **Action item for human: pre-configure trusted publisher BEFORE first `git tag v1.0.0`.**

From 04-RESEARCH.md §Security Domain — OIDC subject claim:
> npm trusted publisher config should bind to a specific workflow file path (`.github/workflows/release.yml`) and `main` branch only — set this on the npmjs.com pending-publisher page.

From 04-RESEARCH.md §Standard Stack — `--provenance` belt-and-suspenders + RESEARCH §Pitfall 1:
> First post-publish, run `npm view keeping-mcp --json | jq '.dist.attestations'` — if this is null, provenance did not attach.

From 04-RESEARCH.md §Pitfall 8 (cold-start smoke on the published tarball):
> Phase 4 plans should add a release-workflow step that re-runs the Phase 1 missing-token smoke AGAINST THE PUBLISHED PACKAGE (post-`npm publish`), to verify the published tarball still has correct behavior. Pull the just-published tarball via `npm pack keeping-mcp@<version>` and run the smoke.
We adapt this to be a Plan 04-04 post-publish task (Task 5), not a workflow step — keeps Plan 04-03 reusable for future releases without baking in 1.0.0-specific verification.

From 04-RESEARCH.md §Pattern 3 + §Pitfall 2 (Windows cold-start surface — SC #5):
> SC #5 cold-start smoke must explicitly use the `cmd /c` form.

From STATE.md Critical Pitfalls:
> 5. OIDC misconfig — verify provenance attestation badge on npm after first publish in Phase 4
</research_excerpts>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Pre-flight — configure npm pending publisher for keeping-mcp (BLOCKING)</name>
  <what-needed>
    npm trusted publishing requires that the package's trusted publisher be configured on the npm settings page BEFORE the first publish — for a brand-new package name like `keeping-mcp`, this MUST be done via the npm "pending publisher" flow (RESEARCH §Pitfall 7). This action has no CLI / API surface accessible to Claude; the user must perform it through npmjs.com's web UI.
  </what-needed>
  <why-human-only>
    Configuring a pending trusted publisher requires (a) being logged in to npm as the human account that will own the package, (b) clicking "Add a pending trusted publisher" in npm account settings, (c) selecting GitHub Actions as the provider, (d) entering the repo and workflow details. There is no npm CLI command for this; the npmjs.com web UI is the only path. Skipping this step makes the first `npm publish` in CI fail with HTTP 403 "no trusted publisher configured".
  </why-human-only>
  <how-to-perform>
    1. Sign in to https://www.npmjs.com as the account that will own `keeping-mcp`.
    2. Navigate to your account avatar → **Account** → **Trusted Publishers**.
    3. Click **Add pending trusted publisher** (do NOT use the per-package settings page — that path requires the package to already exist).
    4. Fill the form with these exact values:
       - **Publisher**: GitHub Actions
       - **Package name**: `keeping-mcp`
       - **Repository owner**: `red-square-software`
       - **Repository name**: `keeping-mcp`
       - **Workflow filename**: `release.yml`
       - **Environment name**: (leave blank — release.yml does not use an environment)
    5. Confirm. The pending publisher will activate automatically the first time `release.yml` runs on a `v*` tag.
    6. (Optional but recommended) On the same page, configure the publisher to restrict to the `main` branch only (per RESEARCH §Security Domain — "npm trusted publisher config should bind to a specific workflow file path and main branch only").

    Verification that the pending publisher is configured:
    - The npm Trusted Publishers page shows a row with `keeping-mcp` + `red-square-software/keeping-mcp` + `release.yml` + status "Pending".
  </how-to-perform>
  <resume-signal>
    Type `pending-publisher-configured` once the row is visible on npmjs.com.
    Type `defer` to abort this plan (no harm done — Plans 04-01/02/03 artifacts remain valid; this plan can be re-run later).
  </resume-signal>
  <done>
    User has explicitly confirmed `pending-publisher-configured` AND has visually verified the row exists on npmjs.com.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Decision — version number for the first release</name>
  <decision>
    Confirm the version number for the first release. RESEARCH and ROADMAP both reference `v1.0.0`, but the current `package.json.version` is `0.1.0` (carried since Phase 1). The version pushed in this plan claims the namespace forever — semver-wise, both options are defensible.
  </decision>
  <context>
    Phase 3 shipped 38/38 v1 requirements (all read tools, all write tools, all timer tools, all safety tools). Per semver, the API surface is feature-complete for v1 scope. ROADMAP, REQUIREMENTS.md, and Phase 4 RESEARCH all reference `v1.0.0` as the target tag.
  </context>
  <options>
    <option id="v1.0.0">
      <name>1.0.0 (recommended)</name>
      <pros>Matches every reference in ROADMAP / REQUIREMENTS / RESEARCH. Signals "v1 scope complete" to users. Semver-correct given 38/38 requirements shipped.</pros>
      <cons>Commits to API stability — any breaking change to tool inputs going forward requires a v2.0.0 bump.</cons>
    </option>
    <option id="v0.2.0">
      <name>0.2.0 (conservative)</name>
      <pros>Signals "pre-1.0, expect possible breakage". Allows backwards-incompatible iteration without forcing v2.</pros>
      <cons>Contradicts ROADMAP "v1.0" milestone wording in STATE.md. Future v1.0.0 bump becomes a coordination point. The npm namespace is already claimed by the first publish regardless of version number — there's no "test version" benefit.</cons>
    </option>
  </options>
  <resume-signal>
    Type `v1.0.0` to accept the recommended option, or `v0.2.0` for the conservative path. Any other version string (e.g. `v1.1.0`) is also accepted but record the rationale in SUMMARY. Plan continues with the chosen tag value substituted for `<VERSION>` in the remaining tasks.
  </resume-signal>
  <done>
    User has explicitly selected a version tag. Substitute it for `<VERSION>` throughout the remaining tasks. Default and recommended: `1.0.0`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bump package.json version + tag + push (triggers release.yml)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current `version` field — confirm starting state is `0.1.0`)
    - .github/workflows/release.yml (the workflow about to be triggered — confirm `on: push: tags: ['v*']`)
    - .planning/phases/04-distribution-release-pipeline/04-03-SUMMARY.md (confirm the "Verify tag matches package.json version" step is present — that step gates the push correctness)
  </read_first>
  <action>
    Use `<VERSION>` from Task 2 (default: `1.0.0`).

    Step 1 — Pre-flight sanity (no state changes):
    - Run `git status` and confirm working tree is clean (no uncommitted changes). If dirty, STOP and surface to user.
    - Run `git branch --show-current` and confirm `main`. If not main, STOP and surface to user.
    - Run `git pull --ff-only origin main` and confirm fast-forwarded (no merge needed).
    - Run `npm run check-publish-shape` and confirm exit 0.
    - Run `npm run build && npm test` and confirm exit 0 (last-mile sanity that the tarball will actually be valid).

    Step 2 — Version bump:
    - Run `npm version <VERSION> -m "release: %s"`. This:
      - Updates `package.json.version` to `<VERSION>`.
      - Updates `package-lock.json.version` to match.
      - Creates a commit with message `release: <VERSION>`.
      - Creates a git tag `v<VERSION>` on that commit.
    - Confirm `node -p "require('./package.json').version"` outputs `<VERSION>`.
    - Confirm `git tag -l "v<VERSION>"` returns the tag.

    Step 3 — Push (irreversible publish trigger):
    - Surface to the user: "About to push commit + tag. This triggers the release workflow and publishes to npm + MCP Registry. Once the tag is on GitHub, the publish is in-flight."
    - Run `git push origin main` (the commit).
    - Run `git push origin v<VERSION>` (the tag — separate push from `main` so the workflow can observe the tag explicitly).
      - Equivalent: `git push --follow-tags`. Prefer the explicit two-command form so failure modes are isolated.

    Step 4 — Watch the workflow:
    - Run `gh run watch --workflow=release.yml` (if `gh` is installed and authenticated). If `gh` is not available, surface the workflow URL `https://github.com/red-square-software/keeping-mcp/actions/workflows/release.yml` for the user to monitor.
    - Wait until the workflow concludes. Surface the outcome (success / failure) and the run URL.

    Step 5 — Diagnose any failure inline:
    - If `ci-gate` matrix fails: blocker is lint/typecheck/test/build/smoke regression — NOT a Phase 4 concern; surface to user and DO NOT retry the tag. User must fix on `main` and push a new tag.
    - If `publish.npm publish` fails with 403 "no trusted publisher configured": Task 1 was skipped or misconfigured. Surface; DO NOT retry until the pending publisher exists.
    - If `publish.mcp-publisher login github-oidc` fails: permissions block placement issue (RESEARCH §Pitfall 5). Inspect `.github/workflows/release.yml` `permissions:` block — must be at the `publish` JOB level. If misplaced, fix in a follow-up plan; the tag is already claimed and the npm publish may have succeeded — DO NOT delete the tag; instead bump to `<VERSION>+1` and re-release after the fix.
    - If `publish.mcp-publisher publish` fails with "namespace verification failed": `package.json.mcpName !== server.json.name` (RESEARCH §Pitfall 6). This should have been caught by `npm run check-publish-shape` in the workflow — investigate why it didn't fail earlier. Same recovery: tag is claimed on npm; fix and re-release with bumped version.

    Do NOT amend or force-push the tag. Tags are immutable once pushed; once `v<VERSION>` is on GitHub the npm version is also claimed.
  </action>
  <verify>
    <automated>node -e "const v = require('./package.json').version; if (!/^\\d+\\.\\d+\\.\\d+/.test(v) || v === '0.1.0') { console.error('package.json version not bumped, still', v); process.exit(1); } console.log('package.json version =', v)"</automated>
  </verify>
  <done>
    - `package.json.version` matches the chosen `<VERSION>`.
    - `git tag -l "v<VERSION>"` returns the tag.
    - Both `git push origin main` and `git push origin v<VERSION>` succeeded.
    - Release workflow run started; final status (success or failure) is recorded.
    - On success: npm package page at https://npmjs.com/package/keeping-mcp shows version `<VERSION>` (may take 1-2 minutes to propagate).
  </done>
</task>

<task type="auto">
  <name>Task 4: Post-publish — verify provenance attestation + MCP Registry discoverability</name>
  <files></files>
  <read_first>
    - .planning/phases/04-distribution-release-pipeline/04-RESEARCH.md §Pitfall 1 (provenance verification command), §Code Examples → Verifying provenance section, §Pattern 1 step explanation of mcp-publisher publish
    - .planning/STATE.md Critical Pitfall #5 (provenance badge verification commitment)
  </read_first>
  <action>
    Use `<VERSION>` from Task 2.

    Verification A — npm provenance attestation (REL-02 / ROADMAP SC #2):
    - Run `npm view keeping-mcp@<VERSION> --json`. Capture the JSON output.
    - From the output, extract `.dist.attestations`. It MUST be a non-null object with at minimum a `url` field pointing to a sigstore attestation bundle.
    - Run `npm audit signatures` from a temporary directory after `npm install keeping-mcp@<VERSION>`. Confirm output reports "1 package has a verified registry signature" (exact wording may vary by npm version; the key signal is no FAIL/UNVERIFIED status).
    - Visit `https://www.npmjs.com/package/keeping-mcp` in a browser and confirm the provenance badge ("Provenance" or "Verified provenance") renders next to the version. Capture a screenshot or record the visible text in SUMMARY.

    Verification B — MCP Registry entry (DIST-05 / ROADMAP SC #3):
    - Run `curl -fsSL "https://registry.modelcontextprotocol.io/v0/servers?search=keeping-mcp"` (or open the URL in a browser).
    - Confirm the response includes at least one server with:
      - `name = "io.github.red-square-software/keeping-mcp"`
      - `version = "<VERSION>"` (the value the jq step wrote into server.json)
      - `packages[0].registryType = "npm"`
      - `packages[0].identifier = "keeping-mcp"`
      - `packages[0].version = "<VERSION>"`
    - If the entry is not yet visible, wait up to 5 minutes (registry indexing latency) and retry. If still absent after 10 minutes, escalate — `mcp-publisher publish` may have silently failed (cross-check the release workflow logs for the publish step exit code).

    Verification C — server.json version derivation (REL-03):
    - From the published npm tarball, extract the just-shipped server.json: `npm pack keeping-mcp@<VERSION>` then `tar -xzf keeping-mcp-<VERSION>.tgz package/server.json` (if server.json shipped in the npm tarball — currently server.json is NOT in `files[]`, so this verification path uses the GitHub commit instead).
    - Alternative: `git show v<VERSION>:server.json | jq '.version, .packages[0].version'` — this shows the committed server.json (placeholders `"0.0.0"`). That is EXPECTED — the workflow's jq step mutates server.json in-runner only, never commits the mutation. The mutation is observable in the MCP Registry entry (Verification B step 1 confirmed it) AND in the release workflow logs (`Inject version into server.json` step's stdout shows `"server.json version set to <VERSION>"`).

    Verification D — package contents (DIST-04 / ROADMAP SC #1 end-to-end):
    - Run `npm pack keeping-mcp@<VERSION>` from a temp directory.
    - Run `tar -tzf keeping-mcp-<VERSION>.tgz | sort` and confirm the output lists exactly:
      ```
      package/LICENSE
      package/README.md
      package/dist/bin/keeping-mcp.js
      package/package.json
      ```
      (the `package/` prefix is the npm tarball convention; the underlying files are the allowlisted four.)
    - No `.env`, no `test/`, no `.github/`, no dotfiles.

    Record all four verification outputs in SUMMARY with the exact command outputs (truncate sigstore attestation bundle URLs to ~80 chars for readability).
  </action>
  <verify>
    <automated>npm view keeping-mcp --json > /tmp/npm-view.json 2>&1 && node -e "const d = JSON.parse(require('fs').readFileSync('/tmp/npm-view.json','utf8')); const v = d['dist-tags'] ? d['dist-tags'].latest : null; if (!v) { console.error('FAIL: no latest version on npm'); process.exit(1); } const vd = d.versions ? d.versions[v] : d; const att = vd && vd.dist ? vd.dist.attestations : (d.dist ? d.dist.attestations : null); if (!att) { console.error('FAIL: no .dist.attestations on keeping-mcp@' + v); process.exit(1); } console.log('OK: provenance attestation present for keeping-mcp@' + v); console.log('attestations url:', att.url || att);"</automated>
  </verify>
  <done>
    - npm `.dist.attestations` is non-null for the published version.
    - npm package page renders the provenance badge.
    - MCP Registry entry at `io.github.red-square-software/keeping-mcp` returns the published version.
    - server.json registry version matches the published npm version (REL-03 end-to-end proven).
    - Tarball contents on npm exactly match the allowlist (DIST-04 end-to-end proven).
    - All outputs captured in SUMMARY.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 5: Windows 11 cold-start smoke against the published package (SC #5)</name>
  <what-built>
    Plan 04-03's `ci-gate` job already runs the cold-start smoke on Windows during CI — but that runs against the local-build dist, not against the published npm tarball. SC #5 demands the cold-start smoke pass on a fresh Windows 11 environment running `npx -y keeping-mcp` against the JUST-PUBLISHED package, using the `cmd /c npx` wrapper documented in README.

    The just-shipped artifact: the published `keeping-mcp@<VERSION>` package on the npm registry, which a user on Windows 11 will install via `npx`. The README's Windows config block specifies `cmd /c npx -y keeping-mcp`. SC #5 says a cold-start smoke "passes" — no ENOENT, no silent hang.
  </what-built>
  <how-to-verify>
    On the user's Windows 11 machine (the project's primary platform per CLAUDE.md):

    1. Open a fresh PowerShell window. Confirm: `$env:KEEPING_TOKEN -eq $null` (or empty). If a previous shell had `KEEPING_TOKEN` set, this verification doesn't test the cold-start surface — close that shell and open a NEW one.
    2. Run the cold-start cmd /c form documented in README:
       ```
       cmd /c "npx -y keeping-mcp"
       ```
       Expected: process exits non-zero within ~10 seconds (download + Node startup time). Stderr contains the byte-locked Phase 1 D-05 message: `[keeping-mcp] Configuration error: KEEPING_TOKEN must not be empty`. Stdout is empty.
    3. NEGATIVE test: confirm there is NO `ENOENT` error, NO Python traceback, NO silent hang lasting > 30 seconds.
    4. POSITIVE smoke: set `$env:KEEPING_TOKEN = "kp_test_FAKE_token_value"` in the SAME PowerShell session, then run:
       ```
       echo '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"manual-smoke","version":"1.0.0"}}}' | cmd /c "npx -y keeping-mcp"
       ```
       Expected: one JSON-RPC frame on stdout containing `"serverInfo":{"name":"keeping-mcp"`. No `kp_test_FAKE_token_value` on stderr (token redaction — D-08 / AUTH-03).
    5. Capture both outputs (raw stderr from step 2, raw stdout from step 4) in the resume signal.
  </how-to-verify>
  <resume-signal>
    Type `cold-start-passed` with a paste of the stderr from step 2 + stdout from step 4 to confirm SC #5 met.
    Type `cold-start-failed: <observation>` if either step fails. On failure, the package is published but unusable on Windows — escalate to a hotfix plan (e.g. 04.1) rather than amending this plan.
  </resume-signal>
  <done>
    - User confirmed `cold-start-passed`.
    - The two expected outputs are recorded verbatim in SUMMARY for the audit trail.
    - SC #5 verification line in SUMMARY explicitly cites the captured outputs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human → npmjs.com pending-publisher page | Step that cannot be automated; misconfiguration here makes the first publish fail with HTTP 403. |
| Local git → GitHub → release workflow | Tag push is the trigger; once pushed, the workflow runs without further human intervention until it succeeds or fails. |
| Published npm tarball → end-user Windows machine | SC #5 cold-start smoke is the end-to-end proof that the public package works. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Spoofing | first npm publish — pending publisher | mitigate | Task 1 BLOCKING checkpoint: human-only step on npmjs.com to configure pending publisher with exact repo + workflow filename. Without this, npm publish 403s and namespace stays unclaimed (recoverable but wastes one release attempt). |
| T-04-04-02 | Tampering | tag/package.json version desync | mitigate | Task 3 uses `npm version <VERSION>` (atomic update of package.json + lock + commit + tag) instead of separate edits. The Plan 04-03 workflow's "Verify tag matches package.json version" step is the second line of defence. |
| T-04-04-03 | Information Disclosure | published tarball contents | mitigate | Task 4 Verification D extracts the published tarball and confirms the four-file allowlist held end-to-end. Failure here means the workflow's `npm run check-publish-shape` step regressed silently. |
| T-04-04-04 | Repudiation | provenance attestation missing on first publish | mitigate | Task 4 Verification A explicitly checks `.dist.attestations` is non-null AND visually confirms the badge on npmjs.com. STATE.md Critical Pitfall #5 commits to this check. |
| T-04-04-05 | Denial of Service | Windows cold-start hangs instead of failing fast | mitigate | Task 5 cold-start smoke on the just-published tarball; explicit NEGATIVE test for ENOENT + silent hang. If this fails, the package is published but unusable on the project's primary platform. |
| T-04-04-06 | Tampering | post-publish jq mutation drift (server.json registry version ≠ npm version) | mitigate | Task 4 Verification B+C cross-check the MCP Registry version against the npm version. RESEARCH §Pitfall 3 desync would surface here. |
| T-04-04-SC | Tampering | npm package supply chain | accept | This plan publishes the package, claiming the supply-chain root. Provenance attestation is the durable trust anchor. |
</threat_model>

<verification>
After Task 5 confirmation:

```
npm view keeping-mcp --json | node -e "const d = JSON.parse(require('fs').readFileSync(0,'utf8')); const v = d['dist-tags'].latest; const att = d.versions[v].dist.attestations; if (!att) process.exit(1); console.log('provenance OK for', v);"
```
Must exit 0.

```
curl -fsSL "https://registry.modelcontextprotocol.io/v0/servers?search=keeping-mcp" | node -e "const r = JSON.parse(require('fs').readFileSync(0,'utf8')); const hit = (r.servers || r.data || []).find(s => s.name === 'io.github.red-square-software/keeping-mcp'); if (!hit) { console.error('not in registry yet'); process.exit(1); } console.log('registry OK for', hit.name, hit.version);"
```
Must exit 0.

```
node -e "const v = require('./package.json').version; if (v === '0.1.0') process.exit(1); console.log('package.json version =', v)"
```
Must exit 0 (version bumped past the pre-Phase-4 0.1.0).

Manual review checklist (record in SUMMARY):
- [ ] Task 1: User confirmed pending-publisher row visible on npmjs.com (with repo + workflow + Pending status).
- [ ] Task 2: Version decision recorded with rationale.
- [ ] Task 3: `git push origin main` and `git push origin v<VERSION>` both succeeded; workflow URL captured; final status recorded.
- [ ] Task 4: All four verifications (provenance, registry entry, server.json derivation, tarball contents) returned the expected shape.
- [ ] Task 5: Cold-start stderr matches `[keeping-mcp] Configuration error: KEEPING_TOKEN must not be empty`; initialize-handshake stdout contains `"serverInfo":{"name":"keeping-mcp"`; fake token NOT echoed on stderr.

All six ROADMAP SC #1..#5 references can now be marked VERIFIED in 04-VERIFICATION.md (verifier's job, not this plan's).
</verification>

<success_criteria>
- npm pending trusted publisher configured on npmjs.com BEFORE the tag push (Task 1 human checkpoint).
- `package.json.version` bumped via `npm version <VERSION>` (Task 3).
- `v<VERSION>` tag pushed to GitHub (Task 3).
- Release workflow ran end-to-end successfully (Task 3 watch).
- npm package `keeping-mcp@<VERSION>` published with provenance attestation (Task 4 Verification A).
- MCP Registry entry visible at `io.github.red-square-software/keeping-mcp@<VERSION>` (Task 4 Verification B — DIST-05 end-to-end).
- server.json version on the registry matches the npm version (Task 4 Verification C — REL-03 end-to-end).
- Published tarball contains exactly LICENSE, README.md, dist/bin/keeping-mcp.js, package.json (Task 4 Verification D — DIST-04 end-to-end).
- Windows 11 cold-start smoke confirms SC #5 against the published artifact (Task 5).
- All outcomes documented in SUMMARY with exact command outputs.
</success_criteria>

<output>
Create `.planning/phases/04-distribution-release-pipeline/04-04-SUMMARY.md` when done. Sections: Objective, Pre-flight (Task 1 outcome — pending-publisher config), Version decision (Task 2 outcome + rationale), Release execution (Task 3 — commit SHA, tag, workflow URL, workflow conclusion, any retry history), Post-publish verification (Task 4 — A/B/C/D outputs verbatim), Windows cold-start verification (Task 5 — captured stderr + stdout), Mapping to ROADMAP SC (which SC is verified by which task), Open follow-ups (any items deferred — e.g. ci.yml @v4 → @v5 cleanup, GitHub Release notes automation), Decision references (Task 2 version choice; any deviation from 04-RESEARCH recommendations).
</output>
