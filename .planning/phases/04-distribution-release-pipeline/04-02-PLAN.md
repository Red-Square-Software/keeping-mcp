---
phase: 04-distribution-release-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [REL-04, REL-05]
requirements_addressed: [REL-04, REL-05]
tags: [documentation, install-ux, windows-config]

must_haves:
  truths:
    - "ROADMAP SC #4 user can act: README contains a Windows-specific Claude Code config block with the literal `cmd /c npx -y keeping-mcp` wrapper alongside the macOS/Linux block. A Windows 11 user can paste either block verbatim and the server will connect (research §Pattern 3, §Pitfall 2)."
    - "ROADMAP SC #4 user can onboard: README contains a six-step token-setup section (Keeping prefs → Show features for developers → Generate token), an env var reference, and one example dry-run transcript that names `keeping_add_entry` + `confirm: true` (the SC-required surfaces)."
    - "ROADMAP SC #5 safety surface is prominent: README contains a callout block at the top that warns `KEEPING_REQUIRE_CONFIRM=false` disables the dry-run gate and writes are immediate. The literal string `KEEPING_REQUIRE_CONFIRM=false` appears inside a blockquote (`> ⚠ ...`) and is grep-able from the file's first 60 lines."
    - "Two-block redundancy: the dry-run warning appears twice — once at the top (above-the-fold call-out) and once inside the Configuration section — so a user skimming to Configuration cannot miss it (RESEARCH §Pitfall 2 / REL-05 'front-and-centre' wording)."
  artifacts:
    - path: "README.md"
      provides: "Distribution-ready public README covering SC #4 + SC #5"
      min_lines: 150
      contains: "KEEPING_REQUIRE_CONFIRM=false"
  key_links:
    - from: "README.md Configuration → Windows block"
      to: "claude_desktop_config.json / .mcp.json on user's machine"
      via: "literal JSON snippet user pastes"
      pattern: "cmd.*/c.*npx.*-y.*keeping-mcp"
    - from: "README.md token-setup section"
      to: "Keeping web app Preferences → Show features for developers"
      via: "step-by-step prose"
      pattern: "Show features for developers"
    - from: "README.md dry-run warning callout"
      to: "Phase 3 KEEPING_REQUIRE_CONFIRM runtime gate"
      via: "documented env var name"
      pattern: "KEEPING_REQUIRE_CONFIRM=false"
---

<objective>
Rewrite `README.md` from the current 7-line placeholder into the public distribution README that ROADMAP SC #4 and SC #5 demand. The README is the only user-facing artifact the npm + MCP Registry publish surfaces; it must stand alone as an install/setup/safety document.

Purpose: SC #4 demands token-setup steps + Windows AND macOS/Linux config blocks + env var reference + dry-run transcript. SC #5 demands a front-and-centre warning that `KEEPING_REQUIRE_CONFIRM=false` disables the gate. Without this rewrite, the published npm package page shows nothing useful and a Windows user hits RESEARCH §Pitfall 2 (`spawn npx ENOENT`) the first time they paste a Linux-shaped config block.

Output: A single rewritten `README.md` (~150-220 lines) that ships with the npm tarball and renders on the npm package page, the MCP Registry detail page, and GitHub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-distribution-release-pipeline/04-RESEARCH.md
@CLAUDE.md
@README.md
@package.json

<interfaces>
<!-- The tool names and env vars the README documents — all already implemented in Phase 1-3. -->

Tools registered in `src/server.ts` (12 total, sorted alphabetically by name):
- Reads: `keeping_list_entries`, `keeping_me`, `keeping_organisations`, `keeping_projects`, `keeping_tasks`, `keeping_timer_status`
- Writes: `keeping_add_entry`, `keeping_delete_entry`, `keeping_resume_timer`, `keeping_start_timer`, `keeping_stop_timer`, `keeping_update_entry`

Environment variables read by `src/config.ts` (verified by Phase 1 D-05 + Phase 2 D-22):
- `KEEPING_TOKEN` — REQUIRED, personal access token, no default
- `KEEPING_REQUIRE_CONFIRM` — optional, default `true`, when `false` disables write-tool dry-run gate
- `KEEPING_ORG_ID` — optional, pins all calls to one organisation (skips multi-org disambiguation)
- `KEEPING_LOG_LEVEL` — optional, default `"info"`, controls stderr logger verbosity

Cold-start failure message (Phase 1 D-05, byte-locked):
- `[keeping-mcp] Configuration error: KEEPING_TOKEN must not be empty`
</interfaces>

<research_excerpts>
From 04-RESEARCH.md §Pattern 3 (Windows + macOS/Linux config JSON — verbatim):
```
// Windows
{
  "mcpServers": {
    "keeping-mcp": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "keeping-mcp"],
      "env": { "KEEPING_TOKEN": "kp_live_your_token_here" }
    }
  }
}

// macOS / Linux
{
  "mcpServers": {
    "keeping-mcp": {
      "command": "npx",
      "args": ["-y", "keeping-mcp"],
      "env": { "KEEPING_TOKEN": "kp_live_your_token_here" }
    }
  }
}
```

From 04-RESEARCH.md §Code Examples → Token setup README section (recommended copy, 6 steps):
1. Sign in to your Keeping account.
2. Open **Preferences** (top-right menu).
3. Find the section **Show features for developers** and enable it.
4. A new **Personal access tokens** section appears.
5. Click **Generate new token**, name it (e.g. "Claude Code"), and copy the value.
6. Store it as `KEEPING_TOKEN` in your shell environment OR in your Claude Code config `env` block.

From 04-RESEARCH.md §Code Examples → README warning block (REL-05 verbatim copy):
```
> ## ⚠ Writes are dry-run BY DEFAULT
>
> Every write tool (`keeping_add_entry`, `keeping_update_entry`, `keeping_delete_entry`,
> `keeping_start_timer`, `keeping_stop_timer`, `keeping_resume_timer`) returns a
> **preview** unless you pass `confirm: true` in the tool call.
>
> Setting `KEEPING_REQUIRE_CONFIRM=false` in your environment **disables this gate**.
> Writes then happen on the first call — there is no second chance.
>
> Recommendation: **never set `KEEPING_REQUIRE_CONFIRM=false`** unless you are running
> the server in a non-interactive automation context and have explicitly accepted
> the loss of the confirmation step.
```

From 04-RESEARCH.md §Pitfall 2 (Windows ENOENT):
> A user on Windows 11 follows the README, adds the Linux/macOS config block (`command: "npx"`) by mistake, and Claude Code reports "MCP server failed to start" with `spawn npx ENOENT". README MUST show the Windows config block FIRST (or at minimum, prominently next to the Linux block) with the explicit `cmd /c npx -y keeping-mcp` wrapper. Use a callout/warning, not a footnote.

From 04-RESEARCH.md Open Question #3 (dry-run transcript form):
> Recommendation: Hand-crafted JSON example with a comment that it is illustrative. Less maintenance risk; easier to keep stable across version bumps.
</research_excerpts>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite README.md with Windows-first install UX + dry-run warning callouts</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/04-distribution-release-pipeline/04-RESEARCH.md §Pattern 3 (config blocks), §Code Examples (token setup + warning block), §Pitfall 2 (Windows ENOENT rationale), §Open Question #3 (transcript form)
    - .planning/REQUIREMENTS.md REL-04, REL-05, plus REL-02 (mention OIDC/provenance for the trust narrative — see "Verifying provenance" section below)
    - .planning/ROADMAP.md Phase 4 SC #4, SC #5
    - .planning/PROJECT.md (Core value sentence + "What This Is" copy is the source for the README's opening paragraph)
    - README.md (current placeholder — preserve only the title "# keeping-mcp" and the CI badge URL)
  </read_first>
  <action>
    Rewrite the entire `README.md` end-to-end with the section structure below, in this order, with no extra sections. Preserve the existing first two lines (the `# keeping-mcp` title and the CI badge) verbatim. All other current content (the 7-line placeholder) is replaced.

    Section structure (use these exact H2 headings, in this order):

    1. **`# keeping-mcp`** (existing line 1, unchanged)
    2. **CI badge** (existing line 5, unchanged: `![CI](https://github.com/red-square-software/keeping-mcp/actions/workflows/ci.yml/badge.svg)`)
    3. **Lead paragraph (no heading)** — one paragraph sourced verbatim from PROJECT.md "What This Is": "Open-source Model Context Protocol (MCP) server that exposes the Keeping (api.keeping.nl) time-tracking API as tools an AI coding assistant can call. Built for solo developers who use Claude Code (or any MCP-capable client) and want their billable hours logged into Keeping at the end of a session instead of typed in by hand, while keeping Keeping's existing native Jortt invoicing integration intact." Add a second sentence: "Every write tool is dry-run by default — your hours never reach Keeping until you explicitly confirm."

    4. **First dry-run warning callout (top of file, above-the-fold)** — copy the RESEARCH §Code Examples warning block verbatim (the `> ## ⚠ Writes are dry-run BY DEFAULT` blockquote). This is callout #1 of 2 — REL-05 "front-and-centre" wording demands top-of-file prominence.

    5. **`## Install`** — three subsections:
       - **`### Claude Code on Windows 11`** — single fenced ```json``` block with the RESEARCH §Pattern 3 Windows JSON (`command: "cmd"`, `args: ["/c", "npx", "-y", "keeping-mcp"]`, `env: { "KEEPING_TOKEN": "kp_live_your_token_here" }`). Place this block FIRST per RESEARCH §Pitfall 2 directive ("FIRST (or at minimum, prominently next to the Linux block)"). Path note above the block: "Add to `%APPDATA%\\Claude\\claude_desktop_config.json` or to your project's `.mcp.json` file." A one-line note BELOW the block: "The `cmd /c` wrapper is required on Windows — `npx` resolves to `npx.cmd`, and Claude Code's process spawn does not search PATHEXT extensions (see [anthropics/claude-code#58510](https://github.com/anthropics/claude-code/issues/58510))."
       - **`### Claude Code on macOS / Linux`** — single fenced ```json``` block with the RESEARCH §Pattern 3 macOS/Linux JSON. Path note above the block: "Add to `~/Library/Application Support/Claude/claude_desktop_config.json` (macOS) or `~/.config/Claude/claude_desktop_config.json` (Linux), or your project's `.mcp.json`."
       - **`### Other MCP clients`** — one paragraph: "Any MCP-capable client that supports the stdio transport works. The server is started by `npx -y keeping-mcp` and reads `KEEPING_TOKEN` from its environment." Then list discovery: "Discoverable in the [MCP Registry](https://registry.modelcontextprotocol.io/) as `io.github.red-square-software/keeping-mcp`."

    6. **`## Get a Keeping access token`** — verbatim 6-step list from RESEARCH §Code Examples token-setup section. Six numbered list items, byte-identical to the research copy. Then a one-line WARNING note: "The token has full read+write access to your time entries — treat it like a password. Never commit it to git, never paste it into a chat, never read it back from a tool response (`keeping-mcp` never echoes it)."

    7. **`## Configuration`** — table of all four env vars + the second dry-run warning. Table headings: `Variable | Required | Default | Purpose`. Four rows (in this order):
       - `KEEPING_TOKEN | yes | — | Keeping personal access token (created via the section above)`
       - `KEEPING_REQUIRE_CONFIRM | no | true | When true, write tools return a preview unless called with confirm: true. Setting to false disables the gate entirely — see warning below.`
       - `KEEPING_ORG_ID | no | — | Pin all calls to one organisation id. When unset and the token has access to multiple orgs, write tools require explicit organisation_id input per call.`
       - `KEEPING_LOG_LEVEL | no | info | Server stderr log verbosity. Accepts: debug, info, warn, error.`
       Immediately after the table, insert dry-run warning callout #2 — copy the RESEARCH §Code Examples warning block verbatim (same block as section 4). REL-05 phrasing "front-and-centre" is honoured by the doubled appearance: top-of-file PLUS inside the Configuration section a user looking up env vars cannot miss it.

    8. **`## Tools`** — one paragraph: "keeping-mcp registers 12 tools when the server starts. Read tools (`keeping_me`, `keeping_organisations`, `keeping_projects`, `keeping_tasks`, `keeping_list_entries`, `keeping_timer_status`) call Keeping's API without confirmation. Write tools (`keeping_add_entry`, `keeping_update_entry`, `keeping_delete_entry`, `keeping_start_timer`, `keeping_stop_timer`, `keeping_resume_timer`) return a dry-run preview unless invoked with `confirm: true` (default behavior controlled by `KEEPING_REQUIRE_CONFIRM`)." Do NOT list per-tool inputs — the MCP `listTools` response carries them and any LLM client surfaces them automatically.

    9. **`## Dry-run workflow (example transcript)`** — one paragraph explaining the pattern: "Write tools follow a two-step pattern: propose-then-confirm. The first call returns a preview of the HTTP request the server would send; the second call (with `confirm: true`) actually sends it." Then two fenced ```json``` blocks side-by-side. Mark them as illustrative per RESEARCH Open Question #3.
       - First block — preview: the tool input (`keeping_add_entry` with `description: "Phase 4 release prep"`, `hours: 1.5`, `project_id: 123` — NO `confirm` field), then a `---` comment line, then the preview output payload shape: `{ would_post: { method: "POST", url: "/v1/organisations/<org_id>/time_entries", body: { ... } } }`. Add a short prose hint: "// Illustrative; actual field names match Keeping's OpenAPI (see https://developer.keeping.nl)."
       - Second block — confirm: same tool input PLUS `confirm: true`, then the response shape: `{ id: 98765, day: "2026-06-12", hours: 1.5, ... }`. Same illustrative caveat.

    10. **`## Verifying provenance`** — one paragraph + one fenced ```bash``` block. Paragraph: "Releases are published via GitHub Actions OIDC trusted publishing (no long-lived npm tokens). Every release carries an npm provenance attestation linking the published tarball to a specific commit in this repository." Code block:
        ```
        npm audit signatures
        # or, for a single package:
        npm view keeping-mcp --json | jq '.dist.attestations'
        ```

    11. **`## Local development`** — minimal: clone, `npm ci`, `npm test`, `npm run build`, `node dist/bin/keeping-mcp.js` with `KEEPING_TOKEN=...`. Include one note: "All server output goes to stderr — stdout is reserved for MCP JSON-RPC framing. Never add `console.log`."

    12. **`## License`** — single line: "MIT — see [LICENSE](LICENSE)."

    Markdown rules:
    - All code fences use triple-backtick with language tag (` ```json `, ` ```bash `).
    - JSON examples are STRICT JSON (no `//` comments inside the JSON block — comments go OUTSIDE the fence, in prose). RESEARCH §Anti-Patterns explicitly warns: "Don't write README config as JSON5 / with comments — users will paste verbatim."
    - The string `KEEPING_REQUIRE_CONFIRM=false` MUST appear verbatim in BOTH warning callouts (REL-05 grep target).
    - The string `cmd /c npx -y keeping-mcp` MUST appear in the Windows config block (SC #4 grep target).
    - The string `Show features for developers` MUST appear in the token-setup section (REL-04 grep target).
    - LF line endings (project `.gitattributes` enforces); single trailing LF.
  </action>
  <verify>
    <automated>node -e "const fs = require('node:fs'); const r = fs.readFileSync('README.md', 'utf8'); const checks = [{ name: 'Windows cmd /c block', pattern: /\"command\":\s*\"cmd\",\s*\"args\":\s*\[\s*\"\/c\",\s*\"npx\",\s*\"-y\",\s*\"keeping-mcp\"\s*\]/ }, { name: 'macOS/Linux npx block', pattern: /\"command\":\s*\"npx\",\s*\"args\":\s*\[\s*\"-y\",\s*\"keeping-mcp\"\s*\]/ }, { name: 'KEEPING_REQUIRE_CONFIRM=false literal', pattern: /KEEPING_REQUIRE_CONFIRM=false/ }, { name: 'Show features for developers (token setup)', pattern: /Show features for developers/ }, { name: 'mcpName/registry namespace', pattern: /io\.github\.red-square-software\/keeping-mcp/ }, { name: 'two distinct ⚠ dry-run callouts (REL-05 doubled)', pattern: /⚠.*[Dd]ry-run.*BY DEFAULT[\s\S]*⚠.*[Dd]ry-run.*BY DEFAULT/ }, { name: 'top callout in first 60 lines', test: () => r.split('\n').slice(0, 60).join('\n').includes('KEEPING_REQUIRE_CONFIRM=false') }, { name: 'no .log call (CLAUDE.md no-console-log rule applies to README examples)', test: () => !/console\.log/.test(r) }]; for (const c of checks) { const ok = c.pattern ? c.pattern.test(r) : c.test(); if (!ok) { console.error('FAIL:', c.name); process.exit(1); } else { console.log('OK:', c.name); } } console.log('README structure checks passed.');"</automated>
  </verify>
  <done>
    - `README.md` exists with all 12 sections above in order.
    - All eight grep/regex assertions in the verify command pass.
    - File length is ≥ 150 lines (`wc -l README.md` ≥ 150).
    - First 60 lines contain at least one `KEEPING_REQUIRE_CONFIRM=false` mention (the top callout — REL-05 above-the-fold guarantee).
    - The string `KEEPING_REQUIRE_CONFIRM=false` appears exactly TWICE in the file (top callout + Configuration callout) — grep count = 2.
    - `npx biome check README.md` skips (biome doesn't lint markdown), but `node -e "JSON.parse(require('fs').readFileSync('README.md','utf8').match(/```json\n([\s\S]*?)```/g).map(b => b.replace(/```json\n|```/g, '')).filter(x => x.trim().startsWith('{')).map(x => { JSON.parse(x); return true; }))"` — every fenced ```json``` block is strict-JSON-parseable. Run this on each block individually if needed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → user clipboard | Users copy-paste JSON blocks verbatim. A typo in the Windows block silently fails with `spawn npx ENOENT`. A `//` comment inside the JSON block produces a parse error in `claude_desktop_config.json`. |
| README → npm package page | The README ships in the published tarball (per `files: ["README.md"]`). The npm page renders it; the MCP Registry detail page also references it. Wrong claims about provenance or namespace propagate to discovery surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering (user-side config) | README Windows config block | mitigate | Block placed FIRST per RESEARCH §Pitfall 2; explanatory note below the block names the root cause (PATHEXT). Block is strict JSON (no `//` comments inside the fence — RESEARCH §Anti-Patterns rule). |
| T-04-02-02 | Information Disclosure | README token-setup section | mitigate | Section ends with a one-line WARNING: token never echoed, never committed, never read back from tool responses. No example uses a real token value — placeholder `kp_live_your_token_here` only. |
| T-04-02-03 | Repudiation | README provenance claim | mitigate | "Verifying provenance" section gives the literal `npm audit signatures` command users can run to verify the published artifact's origin. Backs the supply-chain claim with a verifiable action. |
| T-04-02-04 | Tampering | README dry-run warning prominence | mitigate | REL-05 "front-and-centre" wording is honoured by DOUBLE placement — top-of-file callout #1 + inside Configuration section callout #2. Grep count of `KEEPING_REQUIRE_CONFIRM=false` MUST equal 2. |
</threat_model>

<verification>
After Task 1 completes:

```
node -e "const r = require('fs').readFileSync('README.md','utf8'); const c = (r.match(/KEEPING_REQUIRE_CONFIRM=false/g) || []).length; if (c !== 2) { console.error('expected 2 occurrences, got', c); process.exit(1); }"
```
Must exit 0 — REL-05 doubled-callout guarantee.

```
wc -l README.md
```
Should report ≥ 150 lines (sanity — file is no longer a 7-line placeholder).

Manual review checklist (record outcomes in 04-02-SUMMARY.md):
- [ ] First Windows config block on the page is the `cmd /c npx -y keeping-mcp` shape (not the macOS/Linux shape).
- [ ] Token-setup section has 6 numbered steps and ends with the "treat it like a password" warning.
- [ ] Configuration table has exactly four rows (KEEPING_TOKEN, KEEPING_REQUIRE_CONFIRM, KEEPING_ORG_ID, KEEPING_LOG_LEVEL).
- [ ] Dry-run transcript section has two fenced ```json``` blocks and the prose calls them illustrative.
- [ ] "Verifying provenance" section contains the literal `npm audit signatures` command.
- [ ] No `console.log` anywhere in the file (defensive — CLAUDE.md applies even to docs).

Also run the strict-JSON-parse check from Task 1's done criteria — every ```json``` fence parses with `JSON.parse`.
</verification>

<success_criteria>
- ROADMAP SC #4 deliverable: README documents token setup, Windows + macOS/Linux config snippets, env var reference, dry-run transcript.
- ROADMAP SC #5 deliverable: README front-and-centre warns that `KEEPING_REQUIRE_CONFIRM=false` disables dry-run. The doubled callout (top + Configuration section) is the "front-and-centre" implementation.
- REL-04 satisfied: all four required content surfaces present (token setup, dual config blocks, env var ref, dry-run transcript).
- REL-05 satisfied: doubled `KEEPING_REQUIRE_CONFIRM=false` callout, grep count = 2, top callout in first 60 lines.
- Windows pitfall closed: `cmd /c npx -y keeping-mcp` block ships FIRST per RESEARCH §Pitfall 2.
- File ≥ 150 lines, every ```json``` block parses with `JSON.parse`.
- No `console.log` introduced.
</success_criteria>

<output>
Create `.planning/phases/04-distribution-release-pipeline/04-02-SUMMARY.md` when done. Sections: Objective, Decisions taken (any deviation from the 12-section structure above — note why), Files modified, Final line count, Grep-count results for the four anchor strings (KEEPING_REQUIRE_CONFIRM=false, cmd /c npx, Show features for developers, io.github.red-square-software/keeping-mcp), Verification command outputs, Notes for Plan 04-04 (which references README sections during post-publish manual verification).
</output>
