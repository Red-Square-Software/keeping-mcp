---
phase: 01-foundation-scaffolding
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/config.ts
  - src/logger.ts
  - bin/keeping-mcp.ts
  - test/logger.test.ts
  - dist/bin/keeping-mcp.js
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - SAFE-01
requirements_addressed:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - SAFE-01
tags:
  - typescript
  - zod
  - logger
  - tdd
  - smoke-test

must_haves:
  truths:
    - "Decisions implemented in this plan: D-04 (src/config.ts reads KEEPING_TOKEN required + KEEPING_REQUIRE_CONFIRM default true + KEEPING_ORG_ID optional via Zod), D-06 (src/logger.ts bare process.stderr.write wrapper, log.debug/info/warn/error, format `[keeping-mcp] [LEVEL] message`), D-07 (KEEPING_LOG_LEVEL env gates level, default info, enum debug|info|warn|error), D-17 (token-leak unit test enforces logger redaction contract; npm pack --dry-run audit deferred to Phase 4)"
    - "User running `node dist/bin/keeping-mcp.js` with `KEEPING_TOKEN` unset gets exit code ≠ 0"
    - "User running `node dist/bin/keeping-mcp.js` with `KEEPING_TOKEN` unset sees the EXACT stderr line `[keeping-mcp] Configuration error: KEEPING_TOKEN must not be empty` (D-05) — assertion is byte-literal grep"
    - "User running `node dist/bin/keeping-mcp.js` with `KEEPING_TOKEN` unset gets EMPTY stdout (D-13 part c — zero bytes)"
    - "Cross-cutting (D-09): no `console.log`, `console.info`, `console.warn`, `console.debug`, or `process.stdout.write` appears in any file under `src/`, `bin/`, or `test/`"
    - "Cross-cutting (D-13): all three smoke-assertion preconditions hold for the built binary (a) exit≠0, (b) exact stderr message, (c) empty stdout — Plan 03 will codify this as the CI assertion"
    - "Vitest unit test `test/logger.test.ts` proves the fake token `kp_test_FAKE_token_value` is REDACTED to `***` in stderr output even when passed as a nested `Authorization: Bearer <token>` object (AUTH-03, D-16)"
    - "`npm run build` produces `dist/bin/keeping-mcp.js` with `#!/usr/bin/env node` as the literal first line (DIST-03)"
    - "`bin/keeping-mcp.ts` does NOT import or call any MCP SDK function — Phase 1 explicitly does NOT boot a server (D-02)"
    - "`src/config.ts` uses Zod 4 `z.stringbool()` for `KEEPING_REQUIRE_CONFIRM` — NOT `z.coerce.boolean()` (Pitfall 1)"
  artifacts:
    - path: "src/config.ts"
      provides: "Zod env schema (KEEPING_TOKEN required, KEEPING_REQUIRE_CONFIRM stringbool default true, KEEPING_ORG_ID optional, KEEPING_LOG_LEVEL enum default info) and `loadConfig()` that exits 1 with the D-05 message on failure"
      exports: ["KeepingConfig", "loadConfig"]
      contains: "KEEPING_TOKEN must not be empty"
    - path: "src/logger.ts"
      provides: "`createLogger(token, level)` factory that writes `[keeping-mcp] [LEVEL] message` to stderr with token redaction at emit step"
      exports: ["LogLevel", "createLogger"]
      contains: "process.stderr.write"
    - path: "bin/keeping-mcp.ts"
      provides: "Entrypoint that calls loadConfig, creates the logger, emits one info line to stderr, exits 0 on success or 1 on missing token. NO MCP server boot."
      contains: "loadConfig"
    - path: "test/logger.test.ts"
      provides: "Vitest test proving token redaction works for both string args and nested object args (D-16, AUTH-03)"
      contains: "kp_test_FAKE_token_value"
    - path: "dist/bin/keeping-mcp.js"
      provides: "Built binary with shebang as first line, ready for CI smoke test in Plan 03"
      contains: "#!/usr/bin/env node"
  key_links:
    - from: "bin/keeping-mcp.ts"
      to: "src/config.ts loadConfig"
      via: "ESM import with .js extension (Node16 resolution)"
      pattern: "import.*loadConfig.*from.*config\\.js"
    - from: "bin/keeping-mcp.ts"
      to: "src/logger.ts createLogger"
      via: "ESM import with .js extension"
      pattern: "import.*createLogger.*from.*logger\\.js"
    - from: "src/config.ts"
      to: "zod v4"
      via: "z.stringbool() — only available in Zod 4 (Pitfall 1)"
      pattern: "z\\.stringbool"
    - from: "src/logger.ts emit()"
      to: "process.stderr.write"
      via: "Token redacted via String.prototype.replaceAll BEFORE write (D-08)"
      pattern: "replaceAll\\(token"
    - from: "tsup banner config"
      to: "dist/bin/keeping-mcp.js first line"
      via: "injected `#!/usr/bin/env node` (DIST-03)"
      pattern: "^#!/usr/bin/env node"
---

<objective>
Write the three TypeScript source files (`src/config.ts`, `src/logger.ts`, `bin/keeping-mcp.ts`) and the token-redaction Vitest test (`test/logger.test.ts`), then run the full local toolchain (lint, typecheck, test, build, local smoke) to prove every Phase 1 success-criteria invariant holds before CI.

Purpose: Deliver the runtime contract — a binary that fails fast with the exact D-05 message when `KEEPING_TOKEN` is missing, while keeping stdout completely empty and proving (via a Vitest unit test) that the logger redacts the token from any output. This plan establishes all of AUTH-01/02/03 and SAFE-01 in code. CI in Plan 03 will codify the assertions; here we prove them locally first.

Output: 4 source-controlled files (`src/config.ts`, `src/logger.ts`, `bin/keeping-mcp.ts`, `test/logger.test.ts`) plus `dist/bin/keeping-mcp.js` (build artifact, gitignored). Local smoke test passes. Vitest passes. Biome check still passes (no `console.log` slipped in). `tsc --noEmit` still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-scaffolding/01-CONTEXT.md
@.planning/phases/01-foundation-scaffolding/01-RESEARCH.md
@.planning/phases/01-foundation-scaffolding/01-01-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@package.json
@tsconfig.json
@tsup.config.ts
@biome.json
</context>

<interfaces>
<!-- These are the interfaces this plan CREATES (Phase 2 will consume them). Defined here so Plan 02's executor implements against them. -->

```typescript
// src/config.ts exports
export type LogLevel = "debug" | "info" | "warn" | "error";
export type KeepingConfig = {
  KEEPING_TOKEN: string;                 // required, non-empty
  KEEPING_REQUIRE_CONFIRM: boolean;      // default true (via z.stringbool)
  KEEPING_ORG_ID?: string;               // optional
  KEEPING_LOG_LEVEL: LogLevel;           // default "info"
};
export function loadConfig(): KeepingConfig;  // exits process with code 1 on failure (after writing D-05 message to stderr)

// src/logger.ts exports
export type LogLevel = "debug" | "info" | "warn" | "error";
export function createLogger(
  token: string,
  level?: LogLevel
): {
  debug: (...args: unknown[]) => void;
  info:  (...args: unknown[]) => void;
  warn:  (...args: unknown[]) => void;
  error: (...args: unknown[]) => void;
};
```

The exact bodies are specified in RESEARCH.md §`src/config.ts (complete)` and §`src/logger.ts (complete)`. Phase 2 will wire these into the MCP server bootstrap.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the Vitest test for token redaction FIRST (TDD red), then implement `src/logger.ts` to make it pass (green)</name>
  <files>
    src/logger.ts,
    test/logger.test.ts
  </files>
  <read_first>
    .planning/phases/01-foundation-scaffolding/01-RESEARCH.md,
    .planning/phases/01-foundation-scaffolding/01-CONTEXT.md,
    .planning/research/PITFALLS.md
  </read_first>
  <behavior>
    The logger MUST satisfy three observable behaviors (all asserted by `test/logger.test.ts`):

    - **Behavior 1 — Object redaction (D-16, AUTH-03):** Given `createLogger("kp_test_FAKE_token_value", "error")` and a call `log.error({ headers: { Authorization: "Bearer kp_test_FAKE_token_value" } })`, the captured stderr output MUST NOT contain the substring `kp_test_FAKE_token_value` AND MUST contain `***`.
    - **Behavior 2 — String redaction:** Given the same logger and a call `log.error("Auth: Bearer kp_test_FAKE_token_value")`, captured stderr MUST NOT contain `kp_test_FAKE_token_value` AND MUST contain `***`.
    - **Behavior 3 — Level gating (D-07):** Given `createLogger(token, "info")`, calling `log.debug("anything")` MUST produce zero stderr writes.

    All three tests use `vi.spyOn(process.stderr, "write").mockImplementation(...)` to capture stderr; the test imports nothing from `src/config.ts`, only from `src/logger.js` (.js extension required by Node16 ESM resolution per RESEARCH.md Pitfall 3).
  </behavior>
  <action>
    Implement in TDD order:

    1. **RED — Write `test/logger.test.ts` first.** Copy the content from RESEARCH.md §`test/logger.test.ts (complete)` verbatim. The test imports `createLogger` from `../src/logger.js` (.js extension critical — Pitfall 3). Run `npx vitest run` — it MUST fail because `src/logger.ts` does not exist yet. This is the red state; record that the failure is "Cannot find module ../src/logger" or equivalent (NOT an assertion failure, because the file doesn't exist).

    2. **GREEN — Write `src/logger.ts`** (copy from RESEARCH.md §`src/logger.ts (complete)` verbatim). Critical invariants (D-06, D-07, D-08):
       - Begin file with the comment `// RULE: never call process.stdout.write — stdout is reserved for MCP JSON-RPC` (RESEARCH.md Pitfall 5 prevention).
       - Export `type LogLevel = "debug" | "info" | "warn" | "error"`.
       - Export `createLogger(token: string, level: LogLevel = "info")` returning an object with `debug`, `info`, `warn`, `error` methods.
       - Each method serializes args via `typeof arg === "string" ? arg : JSON.stringify(arg)` joined by space.
       - **Critical:** After serializing, call `.replaceAll(token, "***")` on the result string BEFORE writing (D-08). This must happen even for the object-headers case — `JSON.stringify({headers:{Authorization:"Bearer <token>"}})` produces a string containing the literal token, and `replaceAll` then redacts it. This is what proves D-16 / AUTH-03.
       - Write format: `[keeping-mcp] [${LEVEL_UPPERCASE}] ${redacted}\n` via `process.stderr.write` (D-06 format).
       - LEVEL_ORDER gating: only emit when `LEVEL_ORDER[lvl] >= LEVEL_ORDER[minLevel]`.
       - The file MUST NOT contain `console.log`, `console.info`, `console.warn`, `console.debug`, or `process.stdout.write`. Biome `noConsole` would block the console.* calls; the stdout.write rule is by convention + smoke test (RESEARCH.md Pitfall 5).

    3. Re-run `npx vitest run`. All three tests must pass (green).

    4. Run `npx biome check src/logger.ts test/logger.test.ts` — must exit 0.
    5. Run `npx tsc --noEmit` — must exit 0.

    Notes:
    - Use the Write tool for both files; never heredoc.
    - The literal fake token in the test is `kp_test_FAKE_token_value` per D-16 — do not change it; the CI assertion in Plan 03 may reference it indirectly via the test name. The token value is documented as a test sentinel (Pitfall 2 prevention — RESEARCH.md uses fake-looking tokens by design).
  </action>
  <verify>
    <automated>npx vitest run test/logger.test.ts && ! grep -rn "console\.log\|console\.info\|console\.warn\|console\.debug\|process\.stdout\.write" src/logger.ts test/logger.test.ts && npx biome check src/logger.ts test/logger.test.ts && npx tsc --noEmit && echo "LOGGER + TEST GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `test/logger.test.ts` exists and imports `createLogger` from `../src/logger.js` (.js extension verified by literal grep).
    - `src/logger.ts` exists and contains literal substring `process.stderr.write` (writes go to stderr only).
    - `src/logger.ts` contains literal substring `replaceAll(token` (token redaction at emit, D-08).
    - `src/logger.ts` contains literal substring `[keeping-mcp] [` (output format, D-06).
    - `src/logger.ts` does NOT contain `console.log`, `console.info`, `console.warn`, `console.debug`, or `process.stdout.write` (grep returns no matches — D-09).
    - `test/logger.test.ts` contains literal substring `kp_test_FAKE_token_value` (D-16 fake token).
    - `npx vitest run test/logger.test.ts` exits 0 with all 3 tests passing.
    - `npx biome check src/logger.ts test/logger.test.ts` exits 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Logger implementation passes all 3 behavior tests, biome lint, and typecheck. The TDD red→green cycle is recorded in commit history (one commit for red test, one for green implementation) OR documented in the plan summary if commits are deferred to Plan 03.</done>
</task>

<task type="auto">
  <name>Task 2: Write `src/config.ts` (Zod schema + loadConfig) and `bin/keeping-mcp.ts` (entrypoint), then build and run local smoke test</name>
  <files>
    src/config.ts,
    bin/keeping-mcp.ts,
    dist/bin/keeping-mcp.js
  </files>
  <read_first>
    .planning/phases/01-foundation-scaffolding/01-RESEARCH.md,
    .planning/phases/01-foundation-scaffolding/01-CONTEXT.md,
    .planning/research/PITFALLS.md
  </read_first>
  <action>
    Implement the config loader and entrypoint, then build and locally verify the D-13 smoke contract.

    1. **Write `src/config.ts`** (copy verbatim from RESEARCH.md §`src/config.ts (complete)`). Critical invariants per D-04, D-05:
       - `import { z } from "zod"` (NOT `import * as z` — RESEARCH.md uses named import for Zod 4).
       - `ConfigSchema = z.object({ KEEPING_TOKEN: z.string().min(1, "KEEPING_TOKEN must not be empty"), KEEPING_REQUIRE_CONFIRM: z.stringbool().default(true), KEEPING_ORG_ID: z.string().optional(), KEEPING_LOG_LEVEL: z.enum(["debug","info","warn","error"]).default("info") })`.
       - **MUST use `z.stringbool()`** for `KEEPING_REQUIRE_CONFIRM` — NOT `z.coerce.boolean()` (Pitfall 1 — coerce.boolean returns `true` for the string `"false"`).
       - Export `type KeepingConfig = z.infer<typeof ConfigSchema>`.
       - Export `function loadConfig(): KeepingConfig` that calls `ConfigSchema.safeParse(process.env)`. On `!result.success`, build `messages = result.error.issues.map(i => i.message).join("; ")` and call `process.stderr.write(\`[keeping-mcp] Configuration error: ${messages}\\n\`)` then `process.exit(1)`.
       - When KEEPING_TOKEN is missing or empty string, the first issue message will be `KEEPING_TOKEN must not be empty`, producing the exact D-05 stderr line: `[keeping-mcp] Configuration error: KEEPING_TOKEN must not be empty\n`.
       - Do NOT use `console.log`/`info`/`warn`/`debug` or `process.stdout.write` anywhere in this file.

    2. **Write `bin/keeping-mcp.ts`** (copy verbatim from RESEARCH.md §`bin/keeping-mcp.ts (complete)`). Critical invariants per D-02:
       - First line: `#!/usr/bin/env node` (cosmetic in source; tsup banner is what matters in output).
       - Imports: `import { loadConfig } from "../src/config.js"` and `import { createLogger } from "../src/logger.js"` (`.js` extensions required — Pitfall 3).
       - Body: `const config = loadConfig();` then `const log = createLogger(config.KEEPING_TOKEN, config.KEEPING_LOG_LEVEL);` then `log.info("config loaded, server boot deferred to Phase 2");` then `process.exit(0);`.
       - **MUST NOT import from `@modelcontextprotocol/sdk`** — Phase 1 explicitly does not boot the MCP server (D-02 / D-15). The MCP server import lands in Phase 2.
       - Do NOT use `console.log` or `process.stdout.write`.

    3. **Run `npm run build`.** tsup reads `bin/keeping-mcp.ts`, bundles to `dist/bin/keeping-mcp.js` with the `#!/usr/bin/env node` banner as the literal first line. Expected outcome: command exits 0; `dist/bin/keeping-mcp.js` exists and starts with the shebang. If tsup fails, the most likely cause is a TypeScript error in `src/config.ts` or `bin/keeping-mcp.ts` — fix and rebuild.

    4. **Run the local smoke test (D-13 mechanic).** From repo root, with `KEEPING_TOKEN` unset:
       ```bash
       # bash one-liner equivalent to the CI smoke test (Plan 03 will codify in ci.yml)
       unset KEEPING_TOKEN
       STDOUT=$(node dist/bin/keeping-mcp.js 2>/tmp/keeping_smoke_stderr)
       EXIT_CODE=$?
       STDERR=$(cat /tmp/keeping_smoke_stderr)
       echo "exit=$EXIT_CODE"
       echo "stdout_bytes=$(printf '%s' "$STDOUT" | wc -c)"
       echo "stderr=$STDERR"
       ```
       Required outcomes (D-13):
       - `exit` is NOT 0
       - `stdout_bytes` is 0 (empty)
       - `stderr` contains the literal string `[keeping-mcp] Configuration error: KEEPING_TOKEN must not be empty`
       On Windows PowerShell, use the equivalent: `$env:KEEPING_TOKEN=$null; node dist/bin/keeping-mcp.js 2> $env:TEMP\keeping_smoke_stderr; $code=$LASTEXITCODE; Get-Content $env:TEMP\keeping_smoke_stderr`. Either shell is fine — Plan 03 standardizes via `shell: bash` in the CI workflow.

    5. **Run the full lint + typecheck + test sweep** to confirm nothing regressed: `npx biome check . && npx tsc --noEmit && npx vitest run`. All three exit 0.

    6. Do NOT commit. Plan 03 makes the first commit of the entire Phase 1 deliverable.
  </action>
  <verify>
    <automated>npm run build && test -f dist/bin/keeping-mcp.js && head -1 dist/bin/keeping-mcp.js | grep -qF '#!/usr/bin/env node' && ! grep -rn "console\.log\|console\.info\|console\.warn\|console\.debug\|process\.stdout\.write" src/config.ts bin/keeping-mcp.ts && grep -qF 'z.stringbool' src/config.ts && grep -qF '"KEEPING_TOKEN must not be empty"' src/config.ts && bash -c 'unset KEEPING_TOKEN; STDOUT=$(node dist/bin/keeping-mcp.js 2>/tmp/keeping_smoke_stderr); EXIT_CODE=$?; STDERR=$(cat /tmp/keeping_smoke_stderr); test "$EXIT_CODE" -ne 0 && test -z "$STDOUT" && echo "$STDERR" | grep -qF "[keeping-mcp] Configuration error: KEEPING_TOKEN must not be empty"' && npx biome check . && npx tsc --noEmit && npx vitest run && echo "PHASE 1 LOCAL SMOKE PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `src/config.ts` exists; contains literal substring `z.stringbool` (Pitfall 1 — not `z.coerce.boolean`).
    - `src/config.ts` contains literal substring `"KEEPING_TOKEN must not be empty"` (D-05 message source).
    - `src/config.ts` contains literal substring `[keeping-mcp] Configuration error:` (D-05 prefix).
    - `src/config.ts` does NOT contain `console.log`/`info`/`warn`/`debug` or `process.stdout.write`.
    - `bin/keeping-mcp.ts` exists; first line is `#!/usr/bin/env node`.
    - `bin/keeping-mcp.ts` imports from `../src/config.js` and `../src/logger.js` (`.js` extensions — Pitfall 3).
    - `bin/keeping-mcp.ts` does NOT contain any `@modelcontextprotocol/sdk` import (D-02 — no MCP boot in Phase 1).
    - `bin/keeping-mcp.ts` does NOT contain `console.log`/`info`/`warn`/`debug` or `process.stdout.write`.
    - `npm run build` exits 0; `dist/bin/keeping-mcp.js` exists.
    - First line of `dist/bin/keeping-mcp.js` is literally `#!/usr/bin/env node` (DIST-03 — verified by `head -1 | grep -F`).
    - Local smoke test: with `KEEPING_TOKEN` unset, `node dist/bin/keeping-mcp.js` produces exit≠0, empty stdout, and stderr containing the EXACT literal `[keeping-mcp] Configuration error: KEEPING_TOKEN must not be empty` (D-13 a/b/c).
    - `npx biome check .` exits 0 (full repo, not just changed files).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run` exits 0 (all logger tests still pass after adding config files).
  </acceptance_criteria>
  <done>All 4 source-controlled files exist; build produces bin with shebang; local smoke test asserts all three D-13 invariants; lint+typecheck+test sweep passes cleanly. Ready for Plan 03 to add CI workflow that codifies these assertions on `ubuntu-latest × Node 22/24` and `windows-latest × Node 22/24`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `process.env` → loadConfig | Untrusted env crosses here; Zod schema is the validation gate |
| Logger args → process.stderr | Token-redacting transform runs at emit step (D-08) before write |
| Source files → built bundle | tsup banner injection — shebang must survive bundling intact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure (token leak) | `src/logger.ts` emit() | mitigate | Token captured at construction time and `replaceAll`'d at emit BEFORE `process.stderr.write` (D-08). Vitest unit test in `test/logger.test.ts` proves the fake token never appears in captured stderr, even when passed as a nested `Authorization: Bearer <token>` object (D-16, AUTH-03, RESEARCH.md Pitfall 2). |
| T-02-02 | Tampering (silent boolean parse) | `src/config.ts` `KEEPING_REQUIRE_CONFIRM` | mitigate | `z.stringbool()` from Zod 4 — correctly parses `"false"` as `false`. NEVER `z.coerce.boolean()` (which returns `true` for `"false"` — RESEARCH.md Pitfall 1). |
| T-02-03 | Information Disclosure (stdout pollution) | `src/*`, `bin/*` | mitigate | (1) biome `noConsole` rule blocks `console.log`/`info`/`warn`/`debug`. (2) `process.stdout.write` is forbidden by convention + `// RULE:` comment in logger source (RESEARCH.md Pitfall 5 — no biome rule exists for stdout.write). (3) Local smoke test asserts stdout is exactly empty bytes when binary runs (D-13 part c). (4) Plan 03 codifies this in CI on every push. |
| T-02-04 | Denial of Service (silent startup hang) | `bin/keeping-mcp.ts` | mitigate | Fail-fast: `loadConfig()` exits with code 1 BEFORE any other import resolution side effects (RESEARCH.md §"Fail-fast on missing token"). Subprocess host (Claude Code) sees clean exit + stderr message rather than a hung handshake. |
| T-02-05 | Tampering (shebang corruption) | `dist/bin/keeping-mcp.js` first line | mitigate | tsup `banner: { js: "#!/usr/bin/env node" }` injects the shebang explicitly (RESEARCH.md Pitfall 2). `.gitattributes eol=lf` from Plan 01 prevents CRLF corruption that would silently break the bin on macOS/Linux. Local smoke verifies; CI smoke (Plan 03) verifies on Windows too. |
| T-02-06 | Information Disclosure (test token leak) | `test/logger.test.ts` | accept | Fake token `kp_test_FAKE_token_value` is documented as a non-credential sentinel; obviously-fake pattern per RESEARCH.md §Pitfall 2 guidance. Cannot leak real credentials because no real token is involved. |
</threat_model>

<verification>
After this plan completes, the following commands must succeed from repo root:

```bash
# Files exist
test -f src/config.ts && test -f src/logger.ts && test -f bin/keeping-mcp.ts && test -f test/logger.test.ts && test -f dist/bin/keeping-mcp.js

# D-09 enforcement — no console.log/info/warn/debug or process.stdout.write anywhere in src/, bin/, test/
! grep -r "console\.log\|console\.info\|console\.warn\|console\.debug\|process\.stdout\.write" src/ bin/ test/

# DIST-03 — shebang is first line of built bin
head -1 dist/bin/keeping-mcp.js | grep -qF '#!/usr/bin/env node'

# Pitfall 1 — config uses stringbool, not coerce.boolean
grep -qF 'z.stringbool' src/config.ts
! grep -qF 'z.coerce.boolean' src/config.ts

# D-02 — no MCP SDK import in Phase 1 entrypoint
! grep -qF '@modelcontextprotocol/sdk' bin/keeping-mcp.ts src/config.ts src/logger.ts

# D-13 — smoke test on Linux/macOS (Plan 03 codifies for Windows + matrix)
unset KEEPING_TOKEN
STDOUT=$(node dist/bin/keeping-mcp.js 2>/tmp/smoke_stderr)
EXIT_CODE=$?
STDERR=$(cat /tmp/smoke_stderr)
test "$EXIT_CODE" -ne 0
test -z "$STDOUT"
echo "$STDERR" | grep -qF "[keeping-mcp] Configuration error: KEEPING_TOKEN must not be empty"

# Lint + typecheck + tests still green
npx biome check . && npx tsc --noEmit && npx vitest run
```
</verification>

<success_criteria>
- 4 source files written: `src/config.ts`, `src/logger.ts`, `bin/keeping-mcp.ts`, `test/logger.test.ts`.
- All three Vitest tests in `test/logger.test.ts` pass (token redaction for objects, strings, and level gating).
- `npm run build` produces `dist/bin/keeping-mcp.js` with literal `#!/usr/bin/env node` as the first line.
- Local smoke test (KEEPING_TOKEN unset) yields: exit code ≠ 0, empty stdout, stderr contains EXACT literal `[keeping-mcp] Configuration error: KEEPING_TOKEN must not be empty`.
- No `console.log`/`info`/`warn`/`debug` or `process.stdout.write` in any source file (verified by grep).
- No `@modelcontextprotocol/sdk` import in any Phase 1 source file (D-02 — no MCP boot yet).
- `npx biome check .` and `npx tsc --noEmit` and `npx vitest run` all exit 0.
- No commits made (Plan 03 makes the first commit of the entire Phase 1 deliverable).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-scaffolding/01-02-SUMMARY.md` when done. Record:
- Confirmation that `npx vitest run` shows 3 passing tests in `test/logger.test.ts`
- Local smoke result: exit code, stdout byte count, exact stderr line
- Confirmation that the shebang is the literal first line of `dist/bin/keeping-mcp.js`
- Grep confirmation that no forbidden stdout patterns (`console.log`, `console.info`, `console.warn`, `console.debug`, `process.stdout.write`) appear anywhere in `src/`, `bin/`, `test/`
- Note for Plan 03: confirm the exact stderr message string for the CI assertion (should be the literal D-05 line)
- Any deviations from the RESEARCH.md content sketches (there should be none)
</output>
